# Aliyun H100 Status

## Locations

- Development entry: `nas-h100`
- Repository: `/data/nas_ray/home/zeying.gong/algorithm/repos/OmTrackVLA`
- Storage: Aliyun CPFS mounted at `/data/nas_ray`

The development pod is CPU-only. GPU rendering and evaluation must run through the Aliyun Ray submission path; do not use a long-running development-shell process as a benchmark job.

## Verified evidence

Existing NAS artifacts contain rendered observation sheets for one fixed STT, DT, and AT validation episode. The renderer is `tools/benchmark/render_track_episodes.py`.

This proves that one prior worker configuration could import Habitat and render those episodes. It does not prove that the current Ray endpoint, official model environment, checkpoint, full scene dataset, or full benchmark launcher is ready.

## Before the next GPU smoke

1. Check the live `/opt/miniforge/envs/isaacray/bin/md_ai_kit --help` and `list --cluster aliyun <job.yaml>` behavior.
2. Verify the worker image and Python environment.
3. Verify HM3D/MP3D assets, humanoids, official checkpoint, DINO/SiGLIP/Qwen caches, and writable NAS output.
4. Create one canonical one-GPU job YAML for the maintained official or modular route.
5. Run a bounded smoke and record its Ray Job ID and artifacts in `EXPERIMENTS.csv`.

Earlier incompatible and duplicate job YAML prototypes are preserved under `archive/2026-09/h100_prototypes/`; they are not active launch configurations.
