# OmTrackVLA 感知 × 控制实验计划（更新至 C3，2026-08-08）

## 1. 目标

在训练新模型前，先建立感知与控制的二因素实验矩阵，分别测量感知误差、控制误差及二者
闭环耦合。所有最终结果使用相同 episode、相同距离阈值和相同 first-attempt 口径。

| 编号 | 感知 | 控制 | 作用 | 当前状态 |
|---|---|---|---|---|
| A | Oracle panoptic + GT point goal | Oracle navmesh | Privileged reference | 已完成 |
| B | 模块化 RGB-D detector/ReID | Oracle navmesh | 隔离感知损失 | 已完成 |
| C0 | Oracle GT point goal | direct reactive control | 无建图控制下界 | 已实现 |
| C1 | Oracle GT point goal + depth | 初版 local obstacle-map control | 建图/避障原型 | 已实现 |
| C2 | Oracle GT point goal + depth | 保守二维地图 + A* | Spot 建图校准版 | 已完成校准 |
| C3 | Oracle GT point goal + depth | C2 + 安全规划 + 行人轨迹记忆 | 当前模块化控制版本 | 调试中 |
| D | 模块化 RGB-D detector/ReID | 模块化 reactive/learned control | 可部署闭环系统 | 下一阶段 |

对应分析：

```text
感知差距 = A - B
控制差距 = A - C
完整系统差距 = A - D
感知控制交互 = D - B - C + A
```

## 2. 输入与特权信息定义

### 2.1 Oracle navmesh control

可以读取目标 agent 世界坐标、Habitat navmesh 和 GT coordinate takeover，只作为 reference。

### 2.2 模块化 reactive control

控制器只读取 `TargetObservation` 中的局部 `relative_xy/range/bearing/visible` 和内部历史状态，
不读取 `target_agent`、目标 semantic ID、目标世界坐标或 navmesh shortest path。

C 使用 Oracle perception 提供的精确局部 point goal，即使目标暂时离开相机视野也继续提供
精确相对坐标。因此 C 同时也是 “PointGoal-only（无 RGB 定位误差）+ 模块化控制” baseline。

D 使用 RGB/RGB-D perception；目标丢失时没有 GT point goal，只能停止、搜索或使用历史预测。

## 3. 额外 baseline 与消融

最终论文建议包含：

1. RGB-only VLA：RGB/history + instruction 直接预测动作；
2. RGB-only modular：RGB detector/tracker + 单目距离 + 模块化控制；
3. RGB-D modular：RGB detector/ReID + sensor depth + 模块化控制；
4. PointGoal-only：精确相对目标向量 + 模块化/learned control；
5. no-ReID、no-temporal-memory、no-depth、no-lost-recovery 消融；
6. coordinate takeover 与 stop/search 的特权信息消融。

模块指标包括 detection recall、ID precision/recall、ID switch、range/bearing error 和恢复延迟；
系统指标包括 SR、TR、CR、距离误差、动作平滑度和计算吞吐。

## 4. C：Oracle perception + modular control

新增参数：

```text
--controller oracle-navmesh  # 默认，保持旧实验行为
--controller reactive        # C0：直接 PointGoal reactive
--controller map-reactive    # C1：depth 局部障碍地图 + waypoint
--controller map-reactive-vlfm  # C2/C3：Spot 保守二维地图 + A* + 轨迹记忆
```

全量启动器对应环境变量：

```text
CONTROLLER=oracle-navmesh
CONTROLLER=reactive
CONTROLLER=map-reactive
CONTROLLER=map-reactive-vlfm
```

### 4.1 单 episode 调试

STT：

```bash
CUDA_VISIBLE_DEVICES=0 python -u oracle_modular_batch.py \
  --task stt --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root "$PWD/outputs/c_reactive_debug" \
  --perception oracle --controller reactive \
  --save-steps --no-resume
```

DT：

```bash
CUDA_VISIBLE_DEVICES=0 python -u oracle_modular_batch.py \
  --task dt --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root "$PWD/outputs/c_reactive_debug" \
  --perception oracle --controller reactive \
  --save-steps --no-resume
```

AT：

```bash
CUDA_VISIBLE_DEVICES=0 python -u oracle_modular_batch.py \
  --task at --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root "$PWD/outputs/c_reactive_debug" \
  --perception oracle --controller reactive \
  --save-steps --no-resume
```

视频位于：

```text
outputs/c_reactive_debug/<task>/val/videos/*.mp4
```

### 4.2 三任务全量 Val

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=oracle CONTROLLER=reactive \
TASKS=stt,dt,at SPLITS=val \
RENDER_BACKEND=egl \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/c_oracle_perception_reactive_control_full_val" \
bash eval_oracle_modular_8gpu.sh
```

不要与 A/B 共用 `OUTPUT_ROOT`。结果 JSON 和 CSV 会记录 `controller=reactive`，resume 也会
检查 controller 名称，防止复用其他控制器的 episode。

## 5. C1：Ascent-style obstacle map prototype

C1 复用 Ascent 中有用的核心思路：`depth -> point cloud -> height filter -> obstacle
grid -> robot-radius inflation -> local waypoint`，但不引入 frontier/VLM/多楼层逻辑。

- 静态层：由 depth 投影，排除地板、天花板和机器人自身近场点；
- 动态层：行人当前帧实时更新，不写入 persistent static obstacle；
- 目标人：从静态层剔除，但在动态层保留安全半径，规划目标是距人
  `1.2--1.5 m` 的 follow ring，而不是人体中心；
- 视频：左侧 RGB 第一视角，右侧局部障碍地图；黑色为膨胀障碍，粉色为动态人体，
  红色为机器人，绿色为目标。

C1 已包含 episodic persistent map fusion、dynamic layer 逐帧刷新、robot-radius inflation
和 A* 局部路径。当持久地图因视角噪声暂时堵塞但当前 depth 走廊明显开放时，
控制器会进入 `map_live_depth_recovery`，并以低速重新追踪，避免在椅子/桌子前摆动。
后续仍可加入 dynamic TTL 和速度预测，以支持多行人与快速运动。

### 5.1 单 episode 调试

```bash
CUDA_VISIBLE_DEVICES=0 python -u oracle_modular_batch.py \
  --task stt --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root "$PWD/outputs/c1_map_reactive_debug" \
  --perception oracle --controller map-reactive \
  --save-steps --no-resume
```

### 5.2 三任务全量 Val

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=oracle CONTROLLER=map-reactive \
TASKS=stt,dt,at SPLITS=val \
RENDER_BACKEND=egl \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/c1_oracle_pointgoal_map_control_full_val" \
bash eval_oracle_modular_8gpu.sh
```

## 6. C2/C3：Spot 保守二维建图与轨迹记忆控制

C2 完成了 Spot 深度建图校准；C3 在 C2 基础上加入安全规划、行人历史轨迹和规划稳定性修复。代码参数仍为：

```text
--controller map-reactive-vlfm
```

C3 当前逻辑：

- Spot 相机高度/前向偏移均为 `0.24 m`，HFOV 为 `90°`；
- 障碍高度带 `0.20–1.20 m`，地图 `20 m × 20 m`，分辨率 `10 cells/m`；
- 默认保存整个 episode 的地图记忆，使用保守二维 free-space；
- 静态家具进入持久层，当前所有行人进入逐帧动态层，不固化为墙；
- 近场补充检测只接收 `0.90 m` 内且高度至少 `0.10 m` 的返回，避免漏掉近家具并继续过滤地面；
- 第三栏显示原始障碍地图，但 A* 内部使用 Spot `0.30 m` 安全半径；
- A* 路径前方约 `0.55 m` 选 carrot，最终输出 `(Vx,Vy,Vyaw)`；
- 距离误差控制追赶速度，并带 `0.12 m/s` 有限 catch-up 前馈；
- 记录最近约 24 个可见帧的行人轨迹。当前目标被墙/拐角隔断时，优先选择最新且可达的历史点作为中间目标；跳过机器人后方超过 `0.20 m` 的旧点；中间目标锁定到直线重新畅通，避免每帧切换产生绕路；
- `1.50–1.70 m` 使用迟滞保持，只朝向行人，不启动完整 A*。超过 `1.70 m` 才重新规划，避免近距离目标产生几十至上百格的异常路径。

四联调试视频：

```text
RGB | Depth | Obstacle Map + Planning | Local NavMesh Check
```

第三栏主要图例：橙色为行人历史轨迹，紫色菱形为当前历史中间目标，青/黄色线为 A* 路径，黄色标记为 carrot，红/绿/蓝点分别为 follow goal、当前行人和机器人。

已验证的异常修复：

| Episode/step | 修复前路径 | 修复后 |
|---|---:|---:|
| index 0，step 41 | 147 格 | hold 0 格，随后 direct 3 格 |
| index 100，step 11 | 54 格 | hold 0 格 |
| index 100，step 14–16 | 52–57 格 | hold 0 格 |
| index 100，step 19 | 59 格 | hold 0 格 |
| index 100，step 41 | 38 格 | hold 0 格 |
| index 200，step 59 | 82 格 | hold 0 格，随后 direct 3–5 格 |

短回归输出：

```text
outputs/spot_hysteresis_check_index0
outputs/spot_hysteresis_check_index100
outputs/spot_hysteresis_check_index200
```

### 6.1 C3 单 episode 调试

AT index 49（门框、拐角、历史轨迹）：

```bash
CUDA_VISIBLE_DEVICES=0 \
OMTRACKVLA_XVFB_DISPLAY_NUM=250 \
PYTHONPATH=habitat-lab:. \
./run_xvfb.sh python -u oracle_modular_batch.py \
  --task at --split val --shard-id 0 --num-shards 1 \
  --dataset-index 49 \
  --output-root "$PWD/outputs/c3_at_index49_debug" \
  --perception oracle --controller map-reactive-vlfm \
  --map-robot-radius 0 --map-min-static-hits 1 \
  --navmesh-calibration --save-steps --save-video --no-resume
```

`--map-robot-radius 0` 在 C3 Spot profile 中只表示第三栏显示原始地图，内部 A* 仍使用 `0.30 m` 安全半径。`--navmesh-calibration` 很慢，只用于少量调试 episode，不要用于全量实验。

调试其他任务或 episode 时修改 `--task`、`--dataset-index`、display 和输出目录即可。

### 6.2 C3 指定 episode 并行回归

```bash
PYTHON_BIN="$(command -v python)" \
GPU_LIST=0,1,2,3 DATASET_INDICES=0,49,100,200 \
TASK=at SPLIT=val \
PERCEPTION=oracle CONTROLLER=map-reactive-vlfm \
MAP_MEMORY_FRAMES=0 MAP_ROBOT_RADIUS=0 MAP_MIN_STATIC_HITS=1 \
RENDER_BACKEND=egl SAVE_STEPS=1 \
RUN_ID=c3_at_regression \
OUTPUT_ROOT="$PWD/outputs/c3_at_regression" \
LOG_ROOT="$PWD/logs/c3_at_regression" \
bash eval_oracle_indices_8gpu.sh
```

### 6.3 C3 AT 全量 Val

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=oracle CONTROLLER=map-reactive-vlfm \
TASKS=at SPLITS=val \
RENDER_BACKEND=egl \
MAP_MEMORY_FRAMES=0 MAP_ROBOT_RADIUS=0 MAP_MIN_STATIC_HITS=1 \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/c3_oracle_pointgoal_map_control_full_val" \
bash eval_oracle_modular_8gpu.sh
```

### 6.4 C3 三任务全量 Val

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=oracle CONTROLLER=map-reactive-vlfm \
TASKS=stt,dt,at SPLITS=val \
RENDER_BACKEND=egl \
MAP_MEMORY_FRAMES=0 MAP_ROBOT_RADIUS=0 MAP_MIN_STATIC_HITS=1 \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/c3_oracle_pointgoal_map_control_full_val" \
bash eval_oracle_modular_8gpu.sh
```

全量启动器支持多 GPU/每卡多 worker、自动 resume、损坏 JSON 重跑和结束后自动汇总。A/B/C3 应使用不同 `OUTPUT_ROOT`。

## 7. 后续研发顺序

1. 用户检查 C3 文档和异常路径短回归；
2. 重新完整回归 index 0、49、100、200，冻结 C3 参数；
3. 在 train/dev 的分层 100--200 episode 上稳定 C3；
4. 跑 C3 三任务全量，得到纯控制差距；
5. 将同一 C3 controller 接到 RGB-D perception，得到 D；
6. 根据 A/B/C3/D 判断主要瓶颈；
7. 感知侧用 GT bbox、identity、range/bearing 蒸馏；
8. 控制侧用 Oracle action 做 BC，再用 DAgger/residual RL 处理闭环分布偏移；
9. 冻结方法后使用全新输出目录重跑最终 Val，并做 scene-cluster bootstrap 置信区间。

开发和调参应使用 train/dev，Val 只用于冻结后的最终评测。
