# OmTrackVLA 感知 × 控制实验计划（2026-08-06）

## 1. 进展汇报

### 1.1 算法设计

当前采用“感知 + 控制”的模块化设计，将目标身份识别、目标定位与机器人运动规划解耦，
便于分别分析感知误差、控制误差以及二者在闭环中的耦合影响。

#### （1）感知模块

感知模块负责处理图像、点坐标或二者的混合输入，并输出统一的目标状态表示。

目前支持三类输入：

1. Oracle PointGoal：读取目标相对机器人的精确局部坐标，用于排除感知误差；
2. RGB/RGB-D：通过 person detector、OSNet ReID、时序关联和深度估计定位目标；
3. 混合输入：视觉负责目标身份与可见性判断，深度或 PointGoal 提供距离和方向。

统一输出为 `TargetObservation`：

```text
relative_xy = (target_forward, target_left)
range_m
bearing_rad
visible
bbox_xyxy
confidence
```

严格来说，感知模块输出的是“目标相对位置”，而不是已经考虑障碍物的最终路径点。控制模块
会进一步将目标位置转换为距离行人 `1.2--1.5m` 的 follow goal，并结合障碍地图生成真正
可执行的局部 waypoint。这样可以避免机器人直接规划到人体中心。

多人情况下，所有检测到的行人都会进入动态障碍层，但只有身份匹配的目标行人用于生成
`TargetObservation`。Oracle 设置中为每个 humanoid 分配独立 panoptic instance ID；
RGB-person 设置中使用 detector + ReID + 时序关联选择目标。

#### （2）控制模块

控制模块负责根据目标状态和环境障碍信息输出机器人底盘动作：

```text
a_t = (V_x, V_y, V_yaw)
```

其中 `V_x` 为前后速度，`V_y` 为横向速度，`V_yaw` 为旋转速度。当前实现了两级模块化
控制 baseline：

- C0 direct reactive：直接根据目标距离、横向偏差和朝向误差进行比例控制，不显式建图；
- C1 map-reactive：利用 RGB-D 构建持续更新的 episodic obstacle map，在膨胀占据图上使用
  A* 搜索到 follow goal 的路径，并选择约 `0.55m` 前方的 carrot waypoint 输出连续动作。

C1 地图包含：

```text
static_map    墙、桌椅等持续累积的静态障碍
dynamic_map   当前帧所有可见行人，每帧刷新
inflated_map  按机器人半径膨胀后的规划占据图
```

当 A* 暂时无路可走时，控制器先结合实时 depth 比较前、左、右 clearance：近场仍堵塞时保持
同一侧横向避让；实时前方已经开放时，以低速 `map_live_depth_recovery` 重新追踪目标。

### 1.2 当前完成情况

已完成的主要工作如下：

1. 建立 A/B/C/D 感知 × 控制二因素实验矩阵；
2. 完成 Oracle perception + Oracle navmesh 和 RGB-D perception + Oracle navmesh；
3. 完成 C0 direct reactive baseline，并接入单 episode、全量、多 GPU 和 resume 流程；
4. 完成 C1 persistent obstacle map prototype，包括深度投影、高度过滤、静态/动态分层、
   机器人半径膨胀、A*、carrot waypoint 和 blocked recovery；
5. 视频支持 RGB、深度图和 obstacle map 三联画；
6. JSON 每步记录 perception、地图、路径、可见人数和动作诊断；
7. 支持自动汇总 CSV/JSON、进度与 ETA、损坏 JSON 重跑和按 controller 隔离 resume。

C0 的三个本机单 episode 回归均成功：

| 任务 | Success | TR | Collision |
|---|---:|---:|---:|
| STT val0 | 1.0 | 1.0000 | 0.0 |
| DT val0 | 1.0 | 0.9032 | 0.0 |
| AT val0 | 1.0 | 0.9836 | 0.0 |

C1 在 STT val0 和 AT val0 上已经实现成功且无碰撞，证明 persistent mapping、A* 和连续控制
链路可以工作；但在更困难的长轨迹、狭窄空间和多人 episode 上仍存在跟丢或碰撞，因此当前
状态应定义为 prototype，不能仅凭 val0 宣称控制问题已经解决。

### 1.3 当前结论与下一步

当前结果说明模块化接口和基本闭环已经打通。下一阶段重点不是继续针对单个 val episode
调参，而是建立 train/dev 分层回归集，验证跨场景稳定性：

1. 在 100--200 个 train/dev episode 上按任务、场景、轨迹长度、`human_num` 和障碍密度分层；
2. 补充真正“多人同时入镜”的身份保持测试，统计 ID switch、目标 precision/recall 和恢复时间；
3. 改进 obstacle map 的 free-space ray clearing、rolling map、静态证据衰减、dynamic TTL 和速度预测；
4. 将 C1 接入 RGB-person 感知得到 D，测量完整可部署闭环与 Oracle PointGoal 的差距；
5. 根据 A/B/C/D 结果判断主要瓶颈，再决定训练感知模型、控制策略或二者的联合残差模块。

## 2. 目标

在训练新模型前，先建立感知与控制的二因素实验矩阵，分别测量感知误差、控制误差及二者
闭环耦合。所有最终结果使用相同 episode、相同距离阈值和相同 first-attempt 口径。

| 编号 | 感知 | 控制 | 作用 | 当前状态 |
|---|---|---|---|---|
| A | Oracle panoptic + GT point goal | Oracle navmesh | Privileged reference | 已完成 |
| B | 模块化 RGB-D detector/ReID | Oracle navmesh | 隔离感知损失 | 已完成 |
| C0 | Oracle GT point goal | direct reactive control | 无建图控制下界 | 已实现 |
| C1 | Oracle GT point goal + depth | local obstacle-map control | 隔离建图/避障控制损失 | prototype |
| D | 模块化 RGB-D detector/ReID | 模块化 reactive/learned control | 可部署闭环系统 | 下一阶段 |

对应分析：

```text
感知差距 = A - B
控制差距 = A - C
完整系统差距 = A - D
感知控制交互 = D - B - C + A
```

## 3. 输入与特权信息定义

### 3.1 Oracle navmesh control

可以读取目标 agent 世界坐标、Habitat navmesh 和 GT coordinate takeover，只作为 reference。

### 3.2 模块化 reactive control

控制器只读取 `TargetObservation` 中的局部 `relative_xy/range/bearing/visible` 和内部历史状态，
不读取 `target_agent`、目标 semantic ID、目标世界坐标或 navmesh shortest path。

C 使用 Oracle perception 提供的精确局部 point goal，即使目标暂时离开相机视野也继续提供
精确相对坐标。因此 C 同时也是 “PointGoal-only（无 RGB 定位误差）+ 模块化控制” baseline。

D 使用 RGB/RGB-D perception；目标丢失时没有 GT point goal，只能停止、搜索或使用历史预测。

## 4. 额外 baseline 与消融

最终论文建议包含：

1. RGB-only VLA：RGB/history + instruction 直接预测动作；
2. RGB-only modular：RGB detector/tracker + 单目距离 + 模块化控制；
3. RGB-D modular：RGB detector/ReID + sensor depth + 模块化控制；
4. PointGoal-only：精确相对目标向量 + 模块化/learned control；
5. no-ReID、no-temporal-memory、no-depth、no-lost-recovery 消融；
6. coordinate takeover 与 stop/search 的特权信息消融。

模块指标包括 detection recall、ID precision/recall、ID switch、range/bearing error 和恢复延迟；
系统指标包括 SR、TR、CR、距离误差、动作平滑度和计算吞吐。

## 5. C：Oracle perception + modular control

新增参数：

```text
--controller oracle-navmesh  # 默认，保持旧实验行为
--controller reactive        # C0：直接 PointGoal reactive
--controller map-reactive    # C1：depth 局部障碍地图 + waypoint
```

全量启动器对应环境变量：

```text
CONTROLLER=oracle-navmesh
CONTROLLER=reactive
CONTROLLER=map-reactive
```

### 5.1 单 episode 调试

STT：

```bash
CUDA_VISIBLE_DEVICES=0 python -u oracle_modular_batch.py \
  --task stt --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root "$PWD/outputs/c_reactive_debug" \
  --perception oracle --controller reactive \
  --save-steps --no-resume
```

DT：

```bash
CUDA_VISIBLE_DEVICES=0 python -u oracle_modular_batch.py \
  --task dt --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root "$PWD/outputs/c_reactive_debug" \
  --perception oracle --controller reactive \
  --save-steps --no-resume
```

AT：

```bash
CUDA_VISIBLE_DEVICES=0 python -u oracle_modular_batch.py \
  --task at --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root "$PWD/outputs/c_reactive_debug" \
  --perception oracle --controller reactive \
  --save-steps --no-resume
```

视频位于：

```text
outputs/c_reactive_debug/<task>/val/videos/*.mp4
```

### 5.2 三任务全量 Val

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=oracle CONTROLLER=reactive \
TASKS=stt,dt,at SPLITS=val \
RENDER_BACKEND=egl \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/c_oracle_perception_reactive_control_full_val" \
bash eval_oracle_modular_8gpu.sh
```

不要与 A/B 共用 `OUTPUT_ROOT`。结果 JSON 和 CSV 会记录 `controller=reactive`，resume 也会
检查 controller 名称，防止复用其他控制器的 episode。

## 6. C1：Ascent-style obstacle map prototype

C1 复用 Ascent 中有用的核心思路：`depth -> point cloud -> height filter -> obstacle
grid -> robot-radius inflation -> local waypoint`，但不引入 frontier/VLM/多楼层逻辑。

- 静态层：由 depth 投影，排除地板、天花板和机器人自身近场点；
- 动态层：行人当前帧实时更新，不写入 persistent static obstacle；
- 目标人：从静态层剔除，但在动态层保留安全半径，规划目标是距人
  `1.2--1.5 m` 的 follow ring，而不是人体中心；
- 视频：左侧 RGB 第一视角，中间为深度图，右侧为持续更新的 episodic obstacle map；
  灰色为原始静态障碍，黑色为膨胀安全区，粉色为动态人体，蓝色为机器人/轨迹，
  绿色为目标，青色为 A* 路径。

C1 已包含 episodic persistent map fusion、dynamic layer 逐帧刷新、robot-radius inflation
和 A* 局部路径。当持久地图因视角噪声暂时堵塞但当前 depth 走廊明显开放时，
控制器会进入 `map_live_depth_recovery`，并以低速重新追踪，避免在椅子/桌子前摆动。
后续仍可加入 dynamic TTL 和速度预测，以支持多行人与快速运动。

### 6.1 单 episode 调试

```bash
CUDA_VISIBLE_DEVICES=0 python -u oracle_modular_batch.py \
  --task stt --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root "$PWD/outputs/c1_map_reactive_debug" \
  --perception oracle --controller map-reactive \
  --save-steps --no-resume
```

### 6.2 三任务全量 Val

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=oracle CONTROLLER=map-reactive \
TASKS=stt,dt,at SPLITS=val \
RENDER_BACKEND=egl \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/c1_oracle_pointgoal_map_control_full_val" \
bash eval_oracle_modular_8gpu.sh
```

## 7. 后续研发顺序

1. 在 train/dev 的分层 100--200 episode 上稳定 C；
2. 跑 C 三任务全量，得到纯控制差距；
3. 将同一 reactive controller 接到 RGB-D perception，得到 D；
4. 根据 A/B/C/D 判断主要瓶颈；
5. 感知侧用 GT bbox、identity、range/bearing 蒸馏；
6. 控制侧用 Oracle action 做 BC，再用 DAgger/residual RL 处理闭环分布偏移；
7. 冻结方法后使用全新输出目录重跑最终 Val，并做 scene-cluster bootstrap 置信区间。

开发和调参应使用 train/dev，Val 只用于冻结后的最终评测。
