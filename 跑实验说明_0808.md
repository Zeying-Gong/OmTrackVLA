# 障碍物建图与 NavMesh 校准记录（0808）

## 1. 目标

本文记录 C2 模块化控制器的障碍物建图调试过程。当前阶段先把深度图到二维障碍物地图的几何关系校准正确，再迁移回 Spot。控制器的目标不是探索 frontier，而是把被跟踪行人在地图中的位置作为导航目标；行人像素不写入持久静态层，而作为每帧更新的动态障碍处理。

本轮采用默认 PointNav 机器人建立可解释的基准，并把深度障碍物地图和 Habitat NavMesh 重采样到完全相同的局部栅格中进行校对。默认机器人验证完成后，Spot 迁移只替换机器人的相机高度、相机前向偏移以及 NavMesh 安全半径，不再改动已经验证的二维建图逻辑。

## 2. 初始问题与定位结果

早期可视化存在以下问题：

- 地面或低处深度点被错误写成障碍物，狭窄通道被封死；
- 累积地图的左右方向与 RGB 画面镜像；
- 同一帧大量深度像素被当成多次时间观测，`min_static_hits` 没有实现“多帧确认”；
- 使用所有深度射线清空二维 free-space 时，高于沙发、桌面或低矮家具的射线会穿过其二维脚印，产生危险的假可通行区域；
- Spot jaw 相机曾按 60° HFOV 投影，但配置中的实际 HFOV 是 90°；
- 相机前向偏移曾被错误地同时施加到行人目标点和 waypoint，导致控制目标偏移；
- A* carrot 被同时用于方向和追赶速度，短 carrot 会让机器人不走；
- 目标很远但 A* 仅返回起点时，没有可靠进入 blocked recovery；
- blocked 状态每帧重选避障方向，可能左右振荡。

以上问题均已在当前实现中修复。

## 3. 最终采用的保守二维建图逻辑

深度图首先依据相机内参、相机高度和俯仰角反投影到机器人局部坐标，再转换到 episode 固定坐标系。持久地图尺寸为 20 m × 20 m，分辨率为 10 cells/m。

最终默认参数和规则如下：

- memory 默认保存整个 episode，即 `map_memory_frames=None`；
- 静态占据证据按帧累计，同一帧多个像素落入同一格只计一次；
- 只有指定高度范围内的端点写入静态障碍层；
- 所有检测到的行人像素从静态层排除，并在动态层逐帧重建，因此多人场景中不会把其他行人永久固化为墙；
- `free_space_all_depth=False`，只让落在二维障碍高度带内的射线贡献 free-space；
- 默认保留全历史占据证据，同时 free-space 证据可以清理早期的偶发误投影；
- 地图越界点在写入数组前统一用栅格范围 mask 过滤；A* 的邻居扩展也逐格检查边界；
- 可视化中图像左侧对应机器人左侧，栅格变换和 carrot 逆变换使用同一符号约定。

这里最关键的是保守二维 free-space。它不会允许一条从家具上方穿过的高空射线把低矮家具的二维占据脚印清除。此前尝试降低最小障碍高度或增大最大障碍高度，对结果影响很小；真正明显改善 index 0 的是关闭 aggressive all-depth clearing。

## 4. 默认 PointNav 机器人基准

默认机器人使用：

```text
camera_height = 1.25 m
camera_forward_offset = 0 m
camera_pitch = 0°
HFOV = 90°
obstacle_height = 0.20–1.20 m
map_memory = full episode
map_inflation = off（用于原始建图校对）
NavMesh agent radius = 0.10 m
```

调试脚本为 `default_pointnav_map_debug.py`。它使用与控制器相同的 `LocalObstacleMap`，输出严格五联图：

1. RGB；
2. Depth；
3. 深度生成的局部 Obstacle Map；
4. 重采样到同一局部栅格的 Habitat NavMesh；
5. 两者的误差图。

最终结果：

| AT val index | observed cells | false obstacle rate | unsafe free rate | navigable IoU | 视频 |
|---:|---:|---:|---:|---:|---|
| 0 | 1776 | 0.08719 | 0.07515 | 0.8040 | `outputs/default_pointnav_index0_conservative_calibration.mp4` |
| 200 | 2370 | 0.27087 | 0.000836 | 0.7893 | `outputs/default_pointnav_index200_conservative_calibration.mp4` |

对比 aggressive all-depth clearing：

| AT val index | 模式 | unsafe free rate | navigable IoU |
|---:|---|---:|---:|
| 0 | aggressive | 0.340 | 0.599 |
| 0 | conservative | 0.075 | 0.804 |
| 200 | aggressive | 0.00718 | 0.7885 |
| 200 | conservative | 0.000836 | 0.7893 |

高度范围消融中，最小高度从 0.20 m 调到 0.05 m、最大高度从 1.20 m 调到 1.80 m 均没有带来明显改善，因此不继续用高度阈值掩盖 free-space 投影问题。

## 5. 同栅格 NavMesh 校对

跟踪评测可增加：

```bash
--navmesh-calibration
```

程序会以 episode 起点为原点，使用同一 forward/left 朝向、地图尺寸和分辨率，把 `pathfinder.is_navigable()` 重采样到 obstacle map 的栅格。指标定义如下：

- `false_obstacle_rate`：预测为障碍、但 NavMesh 认为可通行的比例；
- `unsafe_free_rate`：预测为可通行、但 NavMesh 认为不可通行的比例；
- `navigable_iou`：已观察区域内预测可通行格与 NavMesh 可通行格的 IoU。

误差图颜色：

- 红色：深度地图认为是障碍，但 NavMesh 可通行；
- 蓝色：深度地图认为可通行，但 NavMesh 不可通行；
- 黑色/深灰：双方都认为不可通行；
- 浅绿色：双方都认为可通行。

`--navmesh-calibration` 会逐格查询 NavMesh，只应用于少量 episode 的诊断与论文可视化，不建议在全量评测中开启。NavMesh 也不是绝对真值：它来自场景几何和 agent radius，可能忽略视觉上存在但未进入导航网格的物体。因此指标应和 RGB、Depth、Obstacle Map 五联视频一起检查。

## 6. 控制逻辑同步修复

地图控制器使用 A* 在膨胀后的静态层与当前动态行人层上规划到目标，选择路径前方的 carrot 作为转向方向；追赶速度仍由机器人到被跟踪行人的真实距离误差决定，而不是由 carrot 的短距离决定。

当目标仍远、但 A* 只返回起点时，控制器把状态判为 blocked。进入 blocked recovery 时只选择一次避障侧并短时锁定，避免左右振荡；如果正前方仍有安全余量，则保留小幅前进分量。该修复曾使 AT val index 49 达到 `success=1.0`、`collision=0.0`、`following_rate=0.7123`，但本轮 Spot 迁移校对按要求改用 index 0 和 200。

## 7. Spot 迁移设置

默认机器人验证通过后，迁回 Spot 时保持上述保守二维逻辑、地图分辨率、高度带、全 episode memory、逐帧 hit、坐标约定和 free-space 规则不变，只替换：

```text
Spot camera_height = 0.24 m
Spot camera_forward_offset = 0.24 m
Spot camera_pitch = 0°
Spot HFOV = 90°
Spot NavMesh agent radius = 0.30 m
```

五联校对使用 `--map-robot-radius 0` 显示未经控制膨胀的原始障碍物地图；第五栏在比较时读取 Habitat Spot NavMesh 自身的约 0.30 m agent radius，并以同一半径膨胀预测障碍。这能避免第三栏和第五栏重复应用安全半径，也便于直接判断深度投影本身是否正确。

AT val index 0 命令：

```bash
CUDA_VISIBLE_DEVICES=0 \
OMTRACKVLA_XVFB_DISPLAY_NUM=227 \
PYTHONPATH=habitat-lab:. \
./run_xvfb.sh python -u oracle_modular_batch.py \
  --task at --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root ./outputs/spot_conservative_index0_navmesh \
  --perception oracle --controller map-reactive-vlfm \
  --map-robot-radius 0 --map-min-static-hits 1 \
  --navmesh-calibration --save-steps --save-video --no-resume
```

AT val index 200 命令只需把 `--dataset-index`、display number 和输出目录改为：

```text
--dataset-index 200
OMTRACKVLA_XVFB_DISPLAY_NUM=228
--output-root ./outputs/spot_conservative_index200_navmesh
```

输出视频位于各输出根目录下的 `at/val/videos/`，episode JSON 位于 `at/val/episodes/`。JSON 的 summary 中同时记录最终 NavMesh 校对指标和实际建图参数，便于确认不同机器上的运行配置一致。

## 8. 当前结论与下一步

默认机器人 index 0 和 200 已证明地图左右方向、深度几何、保守二维 free-space 和同栅格 NavMesh 对照链路基本正确。下一步是严格保持该逻辑，仅用 Spot 的相机几何与 0.30 m NavMesh 半径重跑 index 0、200；分别检查五联视频、NavMesh 误差指标、是否移动、是否碰撞和任务成功率，再决定是否把 Spot 控制用的膨胀半径加入正式全量实验。

## 9. Spot 严格迁移实跑结果

实跑前复核发现跟踪控制器仍继承了 1.80 m 的障碍高度上限，而默认机器人最终基准是 1.20 m。为满足“只替换 Spot 相机高度、相机偏移和 NavMesh 半径”的要求，Spot profile 已显式固定为同一套 0.20–1.20 m 保守二维高度带，然后重新运行 index 0 和 200。

| AT val index | steps | success | collision | following rate | false obstacle rate | unsafe free rate | navigable IoU |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 0 | 61 | 1.0 | 0.0 | 0.9836 | 0.09765 | 0.30812 | 0.5660 |
| 200 | 300 | 1.0 | 0.0 | 0.9700 | 0.16416 | 0.11951 | 0.6708 |

最终五联视频：

```text
outputs/spot_conservative_strict_index0_navmesh/at/val/videos/000000_bzCsHPLDztK_ep_1_90ebf7531c.mp4
outputs/spot_conservative_strict_index200_navmesh/at/val/videos/000200_RJaJt8UjXav_ep_5_6173858f47.mp4
```

视频分别为 61 帧和 300 帧，均包含 RGB、Depth、Obstacle Map、Habitat TopDown/NavMesh 和同栅格误差图。两例均正常移动、无碰撞并成功完成任务。index 0 的 `unsafe_free_rate` 仍相对较高，说明视觉二维地图与 0.30 m Spot NavMesh 安全区域仍有局部差异，后续应继续结合五联画面抽查，而不能只依据任务成功率认定建图已经完全等价于 NavMesh。

## 10. 五联图含义与后续布局调整

当前五联图从左到右分别是：RGB、Depth、深度累计障碍物地图、Habitat 全局 TopDownMap、同栅格差异诊断图。

其中容易混淆的是第三栏和第五栏：

- 第三栏 `Obstacle Map` 是控制器实际维护和用于 A* 规划的局部地图。灰色线条是深度累计得到的静态障碍，橙色是机器人历史轨迹，蓝色与绿色标记机器人/导航目标等当前状态。它是算法输出，也是本阶段主要检查对象。
- 第五栏 `ALIGNED` 不是另一套独立障碍物地图。它把第三栏的已观察区域与 Habitat NavMesh 重采样到相同的 episode 局部栅格后叠加，专门显示二者一致与不一致的位置。因此整体轮廓看起来应当与第三栏接近。红色表示“深度地图判为障碍而 NavMesh 可走”，蓝色表示“深度地图判为可走而 NavMesh 不可走”，黑/深灰和浅绿表示双方一致。

第四栏 Habitat `TopDownMap` 是基于完整场景和全局坐标绘制的导航地图，它的显示范围、裁剪、缩放以及已知信息范围都不同于相机深度生成的局部地图。它可以用于观察全局轨迹，但并不是局部深度建图的直接真值；把它放在校准视频中容易让人误以为应与第三栏逐像素对应。

因此后续诊断视频去掉第四栏全局 TopDownMap，不再为了校准而启用对应 measure。保留局部 NavMesh 重采样本身，用更直接的四联布局展示 RGB、Depth、Obstacle Map 和同栅格差异图。NavMesh 只作为近似几何参照，最终仍以 RGB/Depth 可视检查、是否保留真实通道、碰撞率和任务成功率共同判断建图质量。
