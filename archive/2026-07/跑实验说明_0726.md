# Oracle Modular Version 5 全量运行说明（2026-07-26）

## 1. 运行约定

以下三个任务分别启动 STT、DT、AT，每个任务都运行 `train,val` 两个 split。

- 三个命令共用全新的 version 5 输出目录：
  `/robot/robot-research-raw-data-0/user/gzy/omtrackvla_oracle_modular_dataset_eval_v5`
- 每个任务使用独立的 `RUN_ID`、worker 日志目录和 launcher 总日志。
- 每条命令都会占用 GPU 0--7，必须顺序执行，不能同时启动。
- `REQUIRE_100_SUCCESS=1` 会让 `success=0` 的 episode 自动重跑。
- 失败重试会切换 navmesh 避让侧，最多尝试 5 次；超过次数会明确报错并停止。
- 结果支持断点续跑。重复执行同一条命令时，完整且成功的 version 5 episode 会跳过。

进入仓库：

```bash
cd /robot/robot-research-exp-0/user/gzy/OmTrackVLA
```

## 2. STT 全量命令

```bash
set -o pipefail
RUN_ID=oracle_v5_stt_$(date +%Y%m%d_%H%M%S)
mkdir -p logs

RUN_ID="$RUN_ID" \
OUTPUT_ROOT=/robot/robot-research-raw-data-0/user/gzy/omtrackvla_oracle_modular_dataset_eval_v5 \
GPU_LIST=0,1,2,3,4,5,6,7 \
NUM_SHARDS=8 \
TASKS=stt \
SPLITS=train,val \
SAVE_VIDEO=1 \
VIDEO_FPS=8 \
RENDER_BACKEND=egl \
REQUIRE_100_SUCCESS=1 \
MAX_SUCCESS_ATTEMPTS=5 \
bash eval_oracle_modular_8gpu.sh 2>&1 | tee "logs/${RUN_ID}_launcher.log"
```

## 3. DT 全量命令

```bash
set -o pipefail
RUN_ID=oracle_v5_dt_$(date +%Y%m%d_%H%M%S)
mkdir -p logs

RUN_ID="$RUN_ID" \
OUTPUT_ROOT=/robot/robot-research-raw-data-0/user/gzy/omtrackvla_oracle_modular_dataset_eval_v5 \
GPU_LIST=0,1,2,3,4,5,6,7 \
NUM_SHARDS=8 \
TASKS=dt \
SPLITS=train,val \
SAVE_VIDEO=1 \
VIDEO_FPS=8 \
RENDER_BACKEND=egl \
REQUIRE_100_SUCCESS=1 \
MAX_SUCCESS_ATTEMPTS=5 \
bash eval_oracle_modular_8gpu.sh 2>&1 | tee "logs/${RUN_ID}_launcher.log"
```

## 4. AT 全量命令

```bash
set -o pipefail
RUN_ID=oracle_v5_at_$(date +%Y%m%d_%H%M%S)
mkdir -p logs

RUN_ID="$RUN_ID" \
OUTPUT_ROOT=/robot/robot-research-raw-data-0/user/gzy/omtrackvla_oracle_modular_dataset_eval_v5 \
GPU_LIST=0,1,2,3,4,5,6,7 \
NUM_SHARDS=8 \
TASKS=at \
SPLITS=train,val \
SAVE_VIDEO=1 \
VIDEO_FPS=8 \
RENDER_BACKEND=egl \
REQUIRE_100_SUCCESS=1 \
MAX_SUCCESS_ATTEMPTS=5 \
bash eval_oracle_modular_8gpu.sh 2>&1 | tee "logs/${RUN_ID}_launcher.log"
```

## 5. 进度和结果检查

统计已生成的 episode：

```bash
OUTPUT_ROOT=/robot/robot-research-raw-data-0/user/gzy/omtrackvla_oracle_modular_dataset_eval_v5
for task in stt dt at; do
  for split in train val; do
    count=$(find "$OUTPUT_ROOT/$task/$split/episodes" -type f -name '*.json' 2>/dev/null | wc -l)
    echo "$task/$split $count"
  done
done
```

预期数量：

```text
stt/train 7257
stt/val   1405
dt/train  7257
dt/val    1405
at/train  7257
at/val    1405
```

查看某次运行的 worker 日志：

```bash
tail -f logs/oracle_modular_dataset_<RUN_ID>/*.log
```

每个指标失败的 episode 都会打印 `oracle_batch_unsuccessful` 结构化事件，其中包含：

```text
task / split / dataset_index / episode_key
result_path / video_path
success_attempt / max_success_attempts
完整 summary 指标
```

汇总查看某次运行的全部指标失败：

```bash
rg -n -A 30 '"event": "oracle_batch_unsuccessful"' \
  logs/oracle_modular_dataset_<RUN_ID>/*.log
```

执行异常会打印 `oracle_batch_error`，包括结果路径和完整 traceback：

```bash
rg -n -A 40 '"event": "oracle_batch_error"' \
  logs/oracle_modular_dataset_<RUN_ID>/*.log
```

查看 launcher 总日志：

```bash
tail -f logs/<RUN_ID>_launcher.log
```

全部完成后进行统一校验：

```bash
/robot/robot-research-raw-data-0/user/gzy/temp/miniconda3/envs/tracevln_V2/bin/python \
  summarize_oracle_modular.py \
  /robot/robot-research-raw-data-0/user/gzy/omtrackvla_oracle_modular_dataset_eval_v5 \
  --require-100-success
```

最终必须满足：所有组 `completed == expected`、`errors == 0`、`stale == 0`、`SR == 100%`。
