# 数据生成

## linker_FLUX `dev` 跟踪算法与 `datagen` 对齐检查（2026-07-16）

检查版本：`linker_FLUX` `dev` 已快进到 `2d07081`。当前正式运行入口为
`sage_utils/tracking_episode_collection_datagen.py`，实际调用其中的
`DatagenFollowController`；它只是对 `datagen/follow_script/cylinder_follow_v2.py`
的简化改写，并非直接复用同一套控制器。主循环在调用后无条件 `continue`，文件后半段
保留的旧 pursuit/recovery 状态机不会执行。

### 结论

1. **当前跟踪算法与 `datagen` 不一致。** 路径规划半径、动态避障半径等少数参数对齐，
   但 stuck 判定、recovery、路径进度判定、视觉/NavMesh 融合和局部脱困均被大幅简化。
2. **recovery 频繁主要是误触发。** 当前控制器仅观察 10 个控制步：约 0.30 秒内位移
   小于 0.04 m 且平移命令大于 0.15，就进入固定 12 步 recovery；没有要求目标已在
   safe distance 之外，也不检查 tracking-distance/waypoint/path-index 是否仍在进展。
   `datagen` 则只在确实需要追赶时累计 stuck，持续 1.2 秒才 recovery，并会先尝试跳过
   非尖角的卡住 waypoint。
3. **原保存过滤远比 `datagen` 宽松，基本等于全收，现已收紧。** 修改前默认允许
   recovery，且 rate/collision/final-distance 阈值近似无效；现已增加 datagen 的
   `follower_stuck`、`target_low_motion` blocking incident，并默认要求
   `tracking_rate>=0.8`、`visible_rate>=0.8`、零碰撞、无 recovery、最终距离不超过 3m。

现有 `two_scenes_test_20260716_103403` 的 4 条、每条 300-step 样本统计如下：

| Scene / Episode | Recovery 帧 | Recovery 占比 | Tracking rate | Final distance | 当前默认是否保存 |
|---|---:|---:|---:|---:|---|
| `0001_839920 / ep0` | 104 | 34.7% | 0.867 | 1.02 m | 是 |
| `0001_839920 / ep1` | 59 | 19.7% | 0.230 | 3.53 m | 是 |
| `0039_839888 / ep0` | 52 | 17.3% | 0.553 | 6.09 m | 是 |
| `0039_839888 / ep1` | 65 | 21.7% | 0.463 | 7.84 m | 是 |

四条样本全部触发 recovery，且后三条即使 tracking rate 很低或最终距离超过跟踪范围，
按当前默认参数仍会写入训练数据。这说明 recovery 不是偶发现象，过滤也没有兜住低质量样本。

### 跟踪与 Recovery 对照

| 项目 | 当前 `tracking_episode_collection_datagen.py` | `datagen/cylinder_follow_v2.py` | 是否一致 / 影响 |
|---|---|---|---|
| 实现复用 | 独立的约 200 行 `DatagenFollowController` | 完整 follower behavior | 不一致；后续容易继续漂移 |
| stuck 时间 | 10 控制步，约 0.30 秒 | warn 0.8 秒，recovery 1.2 秒 | 不一致；当前明显更敏感 |
| stuck 位移 | 窗口首尾位移 `<0.04m` | 单步移动 `<0.015m`，并累计时间 | 口径不同 |
| “应该移动”门控 | 仅平移 action norm `>0.15` | command speed `>0.1` 且 tracking distance `>safe_max+0.05` | 当前在近距离/调姿时也可能误判 |
| 路径进度 | 不检查 | 同时看目标距离进展、waypoint 距离进展、waypoint index | 当前易把慢速绕障判 stuck |
| recovery 前处理 | 无 | 非尖角路径先尝试 skip stuck waypoint | 当前直接 recovery，触发更多 |
| recovery 目标 | 后、左、右、后左、后右中选可达位移最大者 | 沿当前运动/waypoint 方向生成多方向局部逃逸点 | 近似但不等价 |
| recovery 时长 | 固定 12 控制步，约 0.40 秒 | `max(1.2s, 1.0s)`，到点也可提前结束 | 不一致 |
| recovery 冷却 | 仅用 `recovery_until`，约 0.40 秒 | 独立 cooldown 1.0 秒 | 当前更容易连续触发 |
| 路径维护 | 每个 FOLLOW step 都重新规划到目标 | 路径缓存、重规划条件、尖角保护、NavMesh 线段校验 | 当前抖动和局部卡住风险更高 |
| 视觉融合 | path/target 方向固定 0.35 融合；bbox 仅控制 yaw | 平滑视觉置信度、NavMesh dominant、边缘/拐角保护 | 不一致 |
| 动态避障 | 简单径向排斥，radius 0.45 | 预测碰撞、ORCA/局部开放空间偏置 | 参数部分一致，算法不一致 |
| 安全距离 | too-close 1.0m，safe 1.2--2.0m | 配置驱动 safe min/max、path stop distance 1.0m | 数值大体接近 |
| NavMesh planning radius | 0.55m | 0.55m | 一致 |
| NavMesh motion 校验 | 每步 snap + occupancy free | NavMesh 投影、滑移/hold、snap rejection diagnostics | 目标相同，正式版更完整 |

### 数据过滤对照

| 过滤项 | 当前 collector 默认值/行为 | `datagen` 行为 | 是否一致 / 建议 |
|---|---|---|---|
| Recovery / follower stuck | 默认 `allow_recovery=False`；recovery active 持续 1 秒或 commanded-motion-without-progress 持续 1 秒均记 blocking | 同样作为 `follower_stuck` blocking incident | 已对齐 |
| Tracking rate | 默认最低 0.8 | 无同名 rate 过滤，以 blocking incident 判无效 | collector 额外严格过滤 |
| Visible rate | 默认最低 0.8 | 无同名 episode-rate 过滤 | collector 额外严格过滤 |
| 连续丢失 | 0 表示禁用 | 不以该条件过滤 | 默认一致；如用于视觉跟踪训练，建议显式设合理阈值而非混同 datagen |
| 碰撞 | 默认 `max_collisions=0`；接触力达到阈值即不保存 | `snap_rejected` 写为 collision；blocking 集合目前不含 collision | collector 更严格；事件定义仍需后续统一 |
| 过近碰撞 | `<0.5m` 立即结束并删除 episode 目录 | 依赖 follower 安全距离/碰撞 diagnostics | collector 新增硬过滤，更严格，可保留 |
| 最终距离 | 默认最大 3m | 无同名终点距离过滤 | collector 额外严格过滤 |
| 结束原因 | 默认仅 `max_steps,char_done` | 有 blocking incident 则整条失败/重生成 | 已去掉 `had_recovery` |
| Target low motion | 单步位移 `<=0.001m` 持续 3 秒即 blocking | 相同 | 已对齐 |
| Follower stuck incident | 距目标 `>=5m`、实际速度 `<=0.05m/s`、命令速度 `>=0.2m/s` 持续 1 秒，或 recovery active 持续 1 秒 | 相同 | 已对齐 |
| 输出完整性 | NPZ 存在即认为可 resume | 校验 GoTo commands、tracking frames、incident report 三者均存在且无 blocking incident | 不一致；resume 前也应重新校验质量元数据 |

### 建议改进顺序

截至本次修改，P0 的 stuck/recovery 判定、blocking incident、严格过滤参数及 runner 的
有效 NPZ 完成判定已落地。collector 主循环也已不再调用简化
`DatagenFollowController`，而是将原版
`datagen/follow_script/cylinder_follow_v2.py` 绑定到不可见 oracle follower prim；原版
BehaviorScript 直接负责路径规划、视觉/NavMesh 融合、避障、stuck/recovery 和运动更新，
collector 只把 oracle 位姿/action/diagnostics 镜像到机器人并负责记录、过滤。因此正式
运行路径复用的是同一份 datagen 控制代码，不再是参数近似的第二套实现。

1. **P0：不要继续手工调简化控制器。** 把 `datagen` 正式 follower 的核心控制/诊断抽成
   Isaac behavior 与 collector 都能调用的共享模块；短期至少将 stuck 的
   `should_move + route progress + 1.2s + 1.0s cooldown + waypoint skip` 完整移植。
2. **P0：统一过滤语义。** collector 输出与 `camera_velocities.json` 等价的 diagnostics，
   直接调用 `datagen/utils/rollout_incidents.py`；`follower_stuck` 或
   `target_low_motion` 出现时不保存 NPZ，并将 episode 标为待重生成。
3. **P0：在正式生成脚本中显式传严格阈值。** 在完成 incident 对齐前，至少使用
   `min_tracking_rate=0.8`、`min_visible_rate=0.8`、`max_collisions=0`、
   `max_final_dist=3.0`、不允许 recovery、结束原因仅 `max_steps,char_done`。
   当前 `--allow_recovery` 使用 `store_true, default=True`，命令行无法关闭，应改成
   `BooleanOptionalAction` 或增加 `--no-allow-recovery`。
4. **P1：记录 recovery 次数、持续帧数和原因。** 现在只保存 mode/布尔值，无法区分
   真卡住、近距离调姿误判、NavMesh snap/occupancy 拒绝或路径抖动；这些字段也应写入
   metrics/preview，作为批量回归指标。
5. **P1：加入固定场景回归门槛。** 每次改控制器后固定跑当前两个场景，至少比较
   recovery episode rate、recovery frame rate、tracking rate P10、final distance P90、
   blocking incident rate，不能只看视频是否能生成。

## FLUX 行人跟踪评测设计（2026-07-16）

### 任务接口

FLUX 原生支持 PointNav、ImageNav 和 NoGoal navigation，但动态行人跟踪仍需要明确“目标
人物是谁”和“希望机器人停在哪里”。当前 EVT-Bench 接入使用 FLUX pointgoal policy：

```text
目标人物局部位置 [forward, lateral]
  -> 沿人物方向减去 follow_distance（默认 1.2 m）
  -> FLUX pointgoal trajectory (24 x 3)
  -> 选择一个 waypoint
  -> EVT-Bench base action [longitudinal, lateral, yaw]
```

PointGoal 必须是机器人局部坐标，不应在现实方案中直接读取人物世界位姿。贴近现实的主要
方案是 `RGB detector + ReID/时序 tracker + depth/stereo/LiDAR + 相机外参`：检测目标人物，
用人体 mask 或 bbox 中央区域的稳健深度反投影到相机三维坐标，再变换到机器人坐标系。
单目深度可以作为纯 RGB 版本，但绝对尺度和人体边缘深度更不稳定。短时遮挡时应使用时序
状态预测，不能立即把 PointGoal 清零。

### 三条对照基线

| 基线 | 目标来源 | 作用 |
|---|---|---|
| Oracle PointGoal | 仿真人物位姿变换到机器人局部坐标 | 隔离感知误差，检查 FLUX 轨迹和动作接口上界 |
| Visual PointGoal | detector + ReID/tracker + depth | 主要现实方案 |
| FLUX ImageGoal | 目标人物参考图或目标视角 | 验证 ImageNav 泛化，不作为默认现实方案 |

当前 Visual PointGoal 原型已去掉人物/机器人位姿真值，但 bbox 暂时来自 Habitat panoptic
真值，因此属于“oracle detection + visual depth”，还不是严格的真实视觉系统。下一步应将
bbox 换成学习型人体 detector，并通过 ReID 在多人场景中维持目标身份。

FLUX ImageNav 通常训练为“导航到参考图像的拍摄地点”，与持续移动的人物身份跟踪并不
等价。直接输入人物 crop 会产生训练分布偏移。更现实的 ImageGoal 用法是用参考人物图做
ReID，定位当前帧中的目标人物，再生成 Visual PointGoal；直接调用 FLUX ImageGoal 应作为
额外对照，并明确 checkpoint 是否训练过人物或动态目标。

### 动作接口核查

EVT-Bench `BaseVelAction` 接收归一化 `[longitudinal, lateral, angular]`，随后分别乘配置的
最大速度；其局部线速度为 `[longitudinal, 0, -lateral]`。FLUX 输出的是轨迹 waypoint，
不是可直接执行的速度。当前适配器暂用 `waypoint[index] / action_dt` 后裁剪。必须通过
Oracle PointGoal 固定 episode 核查以下项目：

1. PointGoal 的前向和横向轴、左右符号是否与 FLUX 训练定义一致。
2. FLUX 第三维是航向量、航向角还是角速度，不能未经确认直接除以 `action_dt`。
3. waypoint 是相对增量还是相对当前机器人的累计位置。
4. EVT-Bench action 已在环境中再次乘最大速度，适配器不应把物理 m/s 当成归一化 action。
5. 近距离、目标丢失和低 critic score 时应显式安全停止，避免继续旋转或撞向人物。

运行时使用 `FLUX_POINTGOAL_SOURCE=oracle` 或 `visual` 显式选择目标来源；正式结果必须记录
该字段，禁止将 oracle 结果与视觉结果混合统计。

### Oracle PointGoal 10-step 检查结果

固定 episode `2n8kARJN3HM/227` 的 Oracle PointGoal 测试确认局部目标坐标正确：首步
`raw_goal=[2.300,-0.003] m`，其范数与人物距离一致；减去 1.2 m 跟随距离后得到
`goal=[1.100,-0.002] m`，左右符号也符合画面中人物基本居中的情况。

测试同时确认原动作换算存在严重尺度错误。首步 FLUX waypoint 为
`[0.295,0.017,0.073]`，旧适配器除以 `action_dt=0.2` 后把前向速度裁成 `1.0`，直接作为
EVT-Bench action。`BaseVelAction` 又将该值乘 `longitudinal_lin_speed=15`，实际命令因此是
15 m/s，而不是期望的约 1.47 m/s。机器人两步内从距人物 2.30 m 冲到 1.55 m，9 步时
距离降至 0.43 m 并碰撞；该结果不能用于评价 FLUX policy 质量。

适配器现已将 waypoint/action_dt 得到的物理速度，分别除以 EVT-Bench 配置的最大速度
`[15 m/s, 10 m/s, 6.28 rad/s]` 后再提交归一化 action。对应环境变量为
`FLUX_ENV_MAX_VX/VY/WZ`。仍待确认 FLUX waypoint 第三维的严格语义，以及人物主动靠近时
小于安全距离的后退/避碰策略；PointGoal policy 会把负前向目标裁为零，不能单独承担后退。

使用相同 episode、随机种子和 10-step 上限重跑速度修复版本后，首步前向 action 从旧版
`1.0` 降为 `0.096`，随后有效前向 action 约为 `0.097--0.112`。episode 正常完成且无碰撞，
following rate 从旧版 `5/9=55.56%` 提升到 `9/10=90%`。第 5 步起 FLUX 已停止平移，
人物距离仍从 1.34 m 下降到 0.64 m，说明剩余过近风险来自人物主动靠近，而不是机器人
继续前冲；后退能力或独立 safety shield 可作为后续训练/部署项，不影响本次速度修复结论。

随后运行 3 个不限制步数、由环境自然终止的完整 Oracle PointGoal episode，结果如下：

| Episode | 终止状态 | Steps | Following rate | Collision | 现象 |
|---|---|---:|---:|---:|---|
| `260` | Lost | 63 | 7.94% | 0 | 先接近到约 0.73 m，人物离开后距离持续增至约 7 m |
| `164` | Lost | 41 | 26.83% | 0 | 持续给约 0.10 前向 action，但距离从约 2.1 m 增至 5.4 m |
| `227` | Collision | 11 | 72.73% | 1 | 距离进入 1.2 m 后停止平移，人物继续靠近并碰撞 |

汇总为 `SR=0%`、平均 `TR=35.83%`、`CR=33.33%`。三条均未出现旧版的前向 action
饱和，说明速度归一化修复有效，但当前适配尚不适合直接启动全量 Oracle 指标。Collision
与 PointGoal 不支持后退一致；两条 Lost 则表明人物转向/离开后轨迹执行方向没有跟上目标。
应优先核对 FLUX waypoint 第三维、横向轴和官方 trajectory controller 的执行语义，尤其
不能默认把第三维直接视为 `yaw_angle/action_dt`。确认适配无误后，再区分 policy 的动态目标
泛化不足与需要重新训练的问题。

代码核查确认 FLUX 官方评测不使用 trajectory 第三维：模型在 `policy_network.py` 内已对
预测增量执行反归一化、`/4.0` 和 `cumsum`，输出是 24 个累计 XY 路径点；官方评测将
`trajectory[:, :2]` 变换到世界坐标，再由差速运动学 `MPC_Controller` 求解线速度和角速度，
默认约束均为 0.5（m/s 和 rad/s）。旧适配器取 `waypoint[1]/action_dt` 并将第三维当 yaw
rate 的语义错误现已移除。

EVT-Bench 同步适配改为局部 pure-pursuit：从完整 XY 路径选择约 0.5 m lookahead 点，以
`atan2(y,x)` 计算路径切向，横移为零，再按 EVT-Bench 最大速度归一化。该控制器与官方
MPC 使用相同的 XY 路径和差速运动学，但速度上限必须按 embodiment 配置；避免引入
CasADi 求解依赖，并适配每个仿真 step 都重新规划的当前调用方式。

### XY 路径控制器验证

纯合成路径测试确认：直线路径输出零角速度，正/负横向弯道分别输出正/负角速度，trajectory
第三维被设置为任意值也不会影响动作。使用真实 episode `227` 验证时，前四步输出约
`0.033` 的归一化前向 action，实际物理速度约 `0.5 m/s`，不再出现旧版的 15 m/s 前冲；
第 5 步起目标进入跟随距离后控制器输出零动作，机器人没有继续前进。该 episode 后续因
人物主动靠近而碰撞，说明路径执行尺度/转向接口问题已修正，剩余问题是此前记录的安全
后退与任务终止策略。若保持零动作，环境不会自动成功结束，完整 episode 会一直运行到
默认 300 步，因此后续回归应加入 safety shield 或明确的跟随成功终止条件。

Dingo 示例默认 0.5 m/s 不适用于 EVT-Bench：主人物配置速度为 10，Spot 前向速度上限为
15。0.5 m/s 控制器在精确 episode `2n8kARJN3HM/164` 仍于第 40 步 Lost，因为实际速度
不足以追赶人物。10 m/s 也只与人物速度持平，转弯降速后没有追赶余量，因此 Spot 控制器
进一步提高到环境上限 15 m/s，角速度上限为 3 rad/s；航向误差超过 45 度
时先原地转向，小角度时按 `cos(heading_error)^2` 平滑降速。

使用 scene+episode 精确筛选重跑 `2n8kARJN3HM/164`：旧版为 41 步 Lost、最大距离约
5.43 m；Spot 速度适配后为 127 步 Normal 自然结束、零碰撞、最大距离 3.51 m、距离中位数
2.12 m，72.44% 步数处于 3 m 内。目标一度拉开至 3.5 m 后，机器人能重新追近至约
1.6 m，说明完整 XY 路径到 Spot 速度的方向和尺度已基本合理。最终 following rate 为
40.16%，剩余限制主要是 facing/目标可见性，应继续调转向增益与视角保持，而不是再次改变
轨迹坐标定义。

进一步将追赶速度从 10 m/s 提高到 Spot 环境上限 15 m/s 后，同一 `164` 的人物距离最大值
从 3.51 m 降至 2.69 m，100% 步数都在 3 m 内，证明 10 m/s 的确仍偏慢。episode 在第
87 步、距离 0.33 m 时 Collision；最后 20 步距离从 2.32 m 持续降至 0.33 m，而近距离
FLUX 输出 zero path，机器人无法后退。这一结果将“追赶速度不足”和“近距无法后退”清楚
分离：15 m/s 适合作为当前追赶速度，剩余碰撞应由后退训练或 safety shield 解决，不能再
通过降低全局追赶速度掩盖。

# OmTrackVLA 模型与训练复现（2026-07-16）

## 模型和方法

OmTrackVLA 是一个“单目视频历史 + 当前图像 + 语言指令 -> 短时局部轨迹”的
waypoint planner。它不把动作离散化为语言 token，而是用 Qwen 融合多模态时序特征，
最后用 MLP 一次回归未来局部 waypoint：

```text
历史 31 帧 RGB --+                         +-- 每帧 4 个 coarse token --+
                  +-- DINOv3 + SigLIP -- 特征拼接 --+                          |
当前 RGB ---------+                         +-- 64 个 fine token ---------+
语言指令 ----------------------------------------------------------+
时间/帧类型 TVI token -----------------------------------------------+
                                                                  |
               [text | history coarse | current fine | ACT]       |
                                  |                               |
                             Qwen3-0.6B <--------------------------+
                                  |
                          ACT hidden state
                                  |
                             3 层 MLP head
                                  |
                       8 x [x, y, theta] waypoint
```

视觉端默认使用 `facebook/dinov3-vits16-pretrain-lvd1689m` 和
`google/siglip-so400m-patch14-384`，输入统一为 384x384。两路 patch token 按通道
拼接成默认 1536 维特征，再做 grid average pooling：历史每帧保留 2x2=4 个 token，
当前帧保留 8x8=64 个 token。31 帧历史时共有 124+64=188 个视觉 token。

1536 维视觉特征通过 `LayerNorm -> Linear -> GELU -> Linear` projector 映射到 Qwen hidden
space。每帧 token block 前插入 TVI（Temporal-Viewpoint Indicator），包含可学习的时间、
history/current 类型和 view embedding；也可加入由 `[sin(yaw), cos(yaw)]` 投影的朝向
token，但默认 `use_angle_tvi=False`。Qwen 最后一个 ACT token 的 hidden state 经三层 MLP
输出 8x3 的局部累积位姿 `[x,y,theta]`。

在 EVT-Bench 闭环推理里，每一个仿真 step 都重新规划 8 个 waypoint，当前实现取
`tau[0,1]` 并除以 `dt=0.1` 变成 `[vx,vy,wz]`。这是 receding-horizon 式闭环控制，
而不是一次性执行整条预测轨迹。

## 训练数据如何产生

`make_tracking_data.py` 读取 Habitat rollout 的 `<episode>.mp4` 和每步位姿/速度元数据
`<episode>_info.json`，从视频提取 RGB 帧，然后为每个时刻建立滑动窗口样本。
默认使用过去 31 帧、当前帧和未来 8 步动作，以 `dt=0.1` 积分本体坐标速度：

```text
theta(t+1) = theta(t) + wz(t) * dt
x(t+1) = x(t) + (cos(theta) * vx - sin(theta) * vy) * dt
y(t+1) = y(t) + (sin(theta) * vx + cos(theta) * vy) * dt
```

每个 JSONL 样本包含 `images`、`current`、`instruction`、`trajectory`、`actions`、
`collision` 和 `target_distance`。不过当前 trainer 只识别 `waypoints` 或 `actions`，
不直接读 `trajectory`；对当前生成数据，它会在 loader 中再次积分 `actions` 生成监督
waypoint。`collision` 和 `target_distance` 也尚未进入训练 loss。

视觉特征建议先用 `precache_frames.py` 离线缓存成 FP16 `.pt`；否则缓存缺失时
DataLoader 会在线运行 DINOv3+SigLIP，训练会明显变慢，多 worker 还可能回退到 CPU 编码。

## 训练目标与优化

目前是监督式 waypoint behavior cloning。在 `valid_mask` 指定的 waypoint 上计算 masked MSE：

```text
L_nav = masked_mean((pred_normalized - gt_normalized)^2)
L = beta_nav * L_nav, beta_nav = 10
```

XY 默认除以 `alpha_xy=2.0` 后计算 loss，yaw 不缩放。代码中预留的 `L_QA` 恒为 0，
没有语言生成 loss、collision loss、tracking-distance loss 或轨迹平滑 loss。优化器为 AdamW，
CLI 默认 `lr=2e-5`、`weight_decay=0.01`、梯度裁剪 1.0，可用 BF16 mixed precision 和 DDP，
但没有 scheduler、warmup 或 gradient accumulation。

默认不传 `--freeze_llm` 时会全量微调 Qwen，同时训练 projector、TVI、ACT token 和
planner head；加 `--freeze_llm` 才是只训外接层和动作头。默认 CLI 还会关闭 action head
的 tanh 边界（`--no_tanh_actions=True`），所以实际是无界连续回归。

## 仓库到底提供了什么

| 项目 | 是否提供 | 当前情况 |
|---|---|---|
| 模型定义 | 是 | `model.py` / `train.py`，包含完整 forward 和 waypoint head |
| 训练代码 | 是 | `train.py`，支持 AdamW、BF16、DDP、resume、checkpoint 和验证 |
| 数据生成代码 | 是 | `make_tracking_data.py`，从 Habitat rollout 生成 frames/JSONL |
| 视觉缓存代码 | 是 | `precache_frames.py` 和 `cache_gridpool.py` |
| 预训练 checkpoint | 是 | Hugging Face `omlab/OmTrackVLA-0.6B` |
| 小样例数据 | 是 | 本机 `data/sample`：145 张 JPG、129 个训练窗口、290 个特征缓存文件 |
| 官方完整训练数据 | **否** | README 明确写 `Coming Soon: releasing the full training data` |
| HM3D/MP3D 场景资产 | 否 | 需要分别申请 license 并下载；本机 `data/scene_datasets` 只有空目录 |
| EVT-Bench episode split | 是 | `data/datasets/track/{STT,DT,AT}` 共约 16 MB，是仿真任务定义，**不是 RGB+动作监督训练集** |

因此可以精确复现训练管线，但目前不能仅靠仓库中已发布文件精确复现官方 checkpoint。
要复现模型效果，需要获取 HM3D/MP3D，在 Habitat 中自己生成大量高质量跟随 rollout，
并尽量对齐官方未公开的数据量、场景比例、指令分布、过滤规则和训练 step 数。

## 建议的复现步骤

1. 先用已有 sample 做 1--10 step smoke test，检查数据、缓存、forward、backward 和 checkpoint。
2. 申请并放置 HM3D/MP3D 和 humanoid assets，用 Habitat 大规模产生 rollout。
3. 使用严格的 tracking rate、visibility、collision、stuck/recovery 过滤；劣质专家轨迹会直接变成 BC 监督信号。
4. 对全部帧预计算 DINOv3+SigLIP coarse/fine token，并统计缓存命中率。
5. 先冻结 Qwen 训 projector/head 建立基线，再进行 Qwen 全量微调；两者要分开记录。
6. 训练期间在 held-out scenes 上做 waypoint MSE 验证，最后必须在 EVT-Bench 做闭环 SR/TR/CR 评测。

sample pipeline 的参考命令：

```bash
cd /robot/robot-research-exp-0/user/gzy/OmTrackVLA

# 特征已缓存；若重建缓存：
python precache_frames.py \
  --data_root data/sample \
  --cache_root data/sample/vision_cache \
  --batch_size 8 \
  --image_size 384

# 先冻结 Qwen 做最小 smoke test
python train.py \
  --train_json data/sample/jsonl \
  --cache_root data/sample/vision_cache \
  --out_dir ckpt_sample_frozen_smoke \
  --epochs 1 --max_train_steps 10 \
  --batch_size 2 --n_waypoints 8 --history 31 \
  --lr 2e-5 --mixed_precision --freeze_llm

# 全量微调 Qwen；单卡先从 batch_size=2 开始测显存
python train.py \
  --train_json data/sample/jsonl \
  --cache_root data/sample/vision_cache \
  --out_dir ckpt_sample_fullft_smoke \
  --epochs 1 --max_train_steps 10 \
  --batch_size 2 --n_waypoints 8 --history 31 \
  --lr 2e-5 --mixed_precision
```

DDP 的有效 global batch 为 `batch_size_per_gpu * GPU_count`。例如 8 卡、每卡 batch 8：

```bash
torchrun --standalone --nproc_per_node=8 train.py \
  --distributed \
  --train_json /path/to/full/jsonl \
  --cache_root /path/to/full/vision_cache \
  --out_dir ckpts_full \
  --epochs 2 --batch_size 8 \
  --n_waypoints 8 --history 31 \
  --lr 2e-5 --mixed_precision
```

## GPU 数量和训练时间估算

官方没有公开完整数据集大小、最终训练 step 数、GPU 型号和 wall-clock time，所以无法给出
“官方 checkpoint 就是训了 N 小时”的确定数字。下面是对当前代码的工程估算，不是官方报告：

| 目标 | 建议 GPU | 大致时间 | 说明 |
|---|---:|---:|---|
| sample 管线 smoke test（129 samples） | 1x 24 GB | 数分钟到半小时 | 主要是首次加载模型，不能证明效果 |
| 冻结 Qwen，只训 projector/head | 1x 16--24 GB | 取决于样本数 | 最适合先验证数据和 loss |
| Qwen3-0.6B 全量 BF16 微调 | 1x 24 GB 可从 batch 1--2 尝试；建议 40--80 GB | 取决于样本数 | AdamW 状态+梯度+激活占用显存；当前无 gradient accumulation |
| 中大规模复现 | 4--8x A100 40/80 GB 或同级 GPU | 通常数小时到数天 | DDP 主要提升吞吐，每卡仍保留完整模型/优化器状态 |

可用下式按实际数据量计算：

```text
steps_per_epoch = ceil(num_samples / (batch_per_gpu * num_gpus))
train_hours = steps_per_epoch * epochs * measured_seconds_per_step / 3600
```

例如仅作量级估算：8 卡、每卡 batch 8、全局 batch 64，如果有 100 万个窗口，
每 epoch 约 15,625 step；若实测为 0.5--1.5 s/step，则每 epoch 约 2.2--6.5 小时。
1000 万个窗口在同样条件下就是约 22--65 小时/epoch。这个区间对 GPU、缓存命中率、
文件系统、DDP 通信和 batch size 都很敏感，正式申请机时前应先在目标 GPU 上跑 100--500 step
记录稳态 `step_time`。

另外，数据生成和视觉特征缓存可能比 planner 训练更耗时：需要先跑 Habitat rollout，然后对每一帧
运行 DINOv3+SigLIP，而且缓存磁盘占用不小。本机 sample 中 145 张 JPG 约 3.8 MB，对应视觉
缓存已约 30 MB，约为原 JPG 体积的 8 倍；正式数据上应提前做磁盘容量规划。

## 优化说明

### linker_FLUX 行人跟踪调试交接（2026-07-16 20:30）

仓库：`/robot/robot-research-exp-0/user/gzy/linker_FLUX`，GitHub 分支：`dev`。
当前修复提交：`38a64aa`（包含 `2258002` 对 camera-only 的回退、`a9e5cdb` 的产物守卫和
串行日志保留，保留
`42569ae` 的静止抖动/occupancy 修复、
`98e10b1` 的 trail 持有/0.20m footprint、
`bf13888` 的历史轨迹/未来碰撞预判、
`18bf72b` 的 snap recovery/depth 重命名、
`7f7b6d8` 的窄通道/depth 视频修复、
`c805dbd` 的二点振荡修复及 `114b9fe` 的 incident 修复），已推送到
`https://github.com/hkustgz-linker/FLUX/tree/dev`。跟踪控制以
`datagen/follow_script/cylinder_follow_v2.py` 为 canonical datagen 逻辑，采集入口是
`sage_utils/tracking_episode_collection_datagen.py`。不要改回旧 controller，也不要根据视频猜原因，
应先拉取最新 dev，再检查 `quality.json`、场景 `.log` 和 rejected NPZ 轨迹。

当前统一机器人抽象尺寸为 Dingo 级别：`footprint_radius=0.28m`、
`planning_radius=0.28m`。Go2/G1/Dingo 只保留各自相机高度差异，机器人渲染网格默认隐藏。
失败 episode 不删除，保留 `_REJECTED`、`quality.json`、
`frames/frame_data.rejected.npz` 和 `frames/preview.rejected.json`；成功 episode 使用
`_ACCEPTED` 和标准 `frame_data.npz`。正式采集默认 `max_steps=300`。

最近一次基线日志位于
`linker_FLUX/logs_formal_v3/test_runs/two_scenes_test_20260716_193821`，该运行使用旧一版
`172af6b` 且误设为 `max_steps=600`。四个 episode 的证据如下：

| scene/episode | tracking rate | final distance | 旧 reject 原因 | 结论 |
|---|---:|---:|---|---|
| `0001/ep0` | 0.848 | 1.01m | `robot_in_obstacle` | 跟踪正常且 0 次 snap reject，单帧障碍检测疑似误报 |
| `0001/ep1` | 1.000 | 1.23m | `navmesh_collision` | 跟踪正常，连续 9 帧 NavMesh 投影失败触发退出，阈值过严 |
| `0039/ep0` | 0.205 | 8.29m | tracking rate/final distance | 真正跟踪失败；仅 1 次 snap reject，不支持“半径过大”解释 |
| `0039/ep1` | 0.965 | 1.44m | `target_low_motion` | 跟踪正常，角色结束后静止被错误当成异常 |

`114b9fe` 和 `c805dbd` 做了四项针对性修改：

1. `robot_in_obstacle` 从单帧立即退出改为连续 5 个控制步命中才退出，避免 occupancy
   边界抖动造成误杀。
2. 连续 `snap_rejected` 紧急退出阈值从 8 提高到 20（约 2 秒），碰撞/持续不可通行仍会退出，
   但短暂沿 NavMesh 边界滑动不再直接废弃高质量 episode。
3. `target_low_motion` 判定前读取权威 `omni:scripting:commandsDone`；正常走完记为
   `char_done`，只有没有完成标志的持续静止才是 blocking incident。同时 canonical follower
   增强了同一路点无进展判断，可识别“命令非零、每帧有抖动，但没有接近目标或路点”的情况并触发
   waypoint skip/recovery。
4. `0039/ep0` 的 NPZ 证明机器人约从 step 190 开始在
   `A=(-2.15695,2.37391)` 与 `B=(-2.15803,2.31501)` 两点间逐帧往返：每步约 5.9cm，
   但长期净位移约为零，所以旧检测把抖动累计成了 32.33m 路程。`c805dbd` 在 NavMesh
   所有候选返回分支加入上一投影起点记忆，禁止 `A->B->A` 立即回返；优先选择非回返候选，
   无其他合法候选时停在 B、清空旧路径并强制下一帧重规划。该事件会记录
   `projection_return_candidate_*`/`projection_two_point_cycle_*` 日志，并将累计次数写入
   `quality.json` 的 `projection_cycle_count`。约束只在 `NAV_OMNI` 生效，不影响
   `TOO_CLOSE` 的合法后退。

开发机复跑命令（脚本默认两个 scene、每个两个 episode、`MAX_STEPS=300`）：

```bash
cd /mnt/nvme1/zeyingg/FLUX
git pull --ff-only origin dev
GPU_ID=0 MAX_STEPS=300 SAVE_VIDEO=1 bash logs_formal_v3/test_two_scenes.sh
```

每次运行会新建 `logs_formal_v3/test_runs/two_scenes_test_<时间戳>/`，其中
`run_info.txt` 必须显示当前最新提交（现为 `git_commit=7f7b6d8...`）和计划的
`max_steps=300`；每个 scene 有独立 `.log`，
每个 episode 有 `quality.json` 和 NPZ，因此可以继续准确诊断。复跑后优先检查：

- `0001/ep0` 是否不再因短暂 `robot_in_obstacle` 退出；若仍退出，查看日志是否确实连续 5 帧。
- `0001/ep1` 的 snap reject 是否自行恢复；若达到连续 20 帧，才考虑路径/半径问题。
- `0039/ep1` 是否以 `char_done` 正常结束。
- `0039/ep0` 是否出现 `projection_return_candidate_omni` 或
  `projection_two_point_cycle_omni`；确认位置不再在上述 A/B 两点交替，再检查是否能沿新路径继续追赶。
  若二点循环消失但仍不前进，再结合 `possible_stuck`、`waypoint_skip_stuck` 和
  `stuck_recovery` 日志修路点选择，不应先继续缩小半径。

质量过滤保持 datagen 口径：`min_tracking_rate=0.8`、`min_visible_rate=0.8`、
`max_collisions=0`、默认不接受 recovery、`max_final_dist=3.0m`，允许结束原因仅
`max_steps,char_done`。`RETRY_REJECTED=1` 用于正式脚本重跑已有 rejected episode；默认 resume
会跳过已经带 `_ACCEPTED` 或 `_REJECTED` 标记的 episode。

### 2026-07-17 最新复跑与修复

最新分析日志：`linker_FLUX/logs_formal_v3/test_runs/two_scenes_test_20260717_095701`。
该运行实际使用 `git_commit=c805dbd`、`max_steps=600`，不要误认为是 300。结果：

| scene/episode | tracking rate | 结果/原因 | 判断 |
|---|---:|---|---|
| `0001/ep0` | 0.868 | accepted | 正常 |
| `0001/ep1` | 1.000 | 20 次连续 snap reject，`navmesh_collision` | 没有物理碰撞；0.33m 规划 clearance 在椅子窄通道过于保守 |
| `0039/ep0` | 0.910 | recovery 后 `navmesh_collision` | 原 A-B 循环已命中并打断一次；剩余是另一窄通道的局部投影/clearance 问题 |
| `0039/ep1` | 0.965 | `target_low_motion` | 跟踪、距离均正常；角色到终点后 `commandsDone` 没置位造成误拒绝 |

提交 `7f7b6d8` 的修改：

- 所有机器人仍使用 `footprint_radius=0.28m`，但 `planning_radius` 从 0.33m 降到
  0.28m。即规划不再额外膨胀 5cm，让抽象机器人使用行人 NavMesh 的可通行路线；没有继续缩小
  碰撞 footprint。
- `target_low_motion` 除检查 `commandsDone` 外，还检查角色是否距离 episode 最后一个 `GoTo`
  点不超过 0.25m，并且机器人仍处于有效跟踪距离。满足时记为 `char_done`，否则仍作为异常拒绝。
- `depth_video.mp4` 不再逐帧转 float 和 jet colormap，直接把已有
  `depth/*.png` 的 uint16 灰度帧交给 libx264。MP4 仅用于快速查看；无损公制深度始终以
  `depth/*.png` 为准。目录从提交 `18bf72b` 起由 `depth_mm/` 正式改名为 `depth/`，像素单位仍是毫米。

下一轮 `run_info.txt` 应显示当前最新提交（现为 `git_commit=18bf72b...`）。重点确认 `0001/ep1` 和
`0039/ep0` 是否通过窄通道、snap reject 是否明显减少，以及 `0039/ep1` 是否以
`char_done` accepted。若 depth MP4 编码报 uint16/pixel-format 错误，保留无损 PNG，并改为由
ffmpeg 直接读取 16-bit PNG 序列编码灰度视频，不要恢复 Python colormap。

最新四 episode 日志：`two_scenes_test_20260717_104518`，使用 `7f7b6d8`、
`max_steps=600`，四个均 rejected：`0001/ep0` 是机器人退到障碍边后目标继续靠近，最终
`proximity_collision`；`0001/ep1` 是椅子处 2 次投影循环后连续 20 次 snap reject；
`0039/ep0` 是 recovery 后仍 NavMesh 阻塞且 tracking 仅 0.298；`0039/ep1` tracking 0.938，
无碰撞/recovery/snap，属于正常终点静止误判。

`18bf72b` 将 requested command 与 applied action 分离：snap reject 时 applied action 仍为 0，
但卡住检测保留“控制器要求移动”的事实，因此能在连续 20 次紧急退出前触发 waypoint skip/recovery。
终点兜底也增加“目标累计移动至少 0.5m、持续静止、当前距离有效、running tracking rate 达标”，
用于修复 `0039/ep1`。下一轮必须确认 `run_info.txt` 为 `git_commit=18bf72b...`，并检查
`0001/ep1`/`0039/ep0` 是否出现 `possible_stuck`、`waypoint_skip_stuck` 或 recovery，而不是动作清零后
直接 `navmesh_collision`。

### 历史轨迹主跟随与未来碰撞预判（提交 `bf13888`）

canonical follower 现在保留行人实际走过的 breadcrumb：行人每移动至少 0.2m 记录一个经
NavMesh 投影的点，最多 200 点。提交 `2460806` 曾错误地只在卡住后使用历史点，并把未来点当作
追踪目标；该语义已由 `bf13888` 完全纠正：

1. 机器人正常情况下就沿真实 observed trail 跟随：从机器人最近历史点之后，选择前方至少 0.5m
   的 breadcrumb。索引只向前推进，避免交叉路径上倒退选旧点；历史点不足时才暂追当前人。
2. collector 从 episode `GoTo.path` 计算约 1.5s 后的未来点，但未来点绝不作为追踪目标。
3. 只有未来点比当前人更接近机器人（人在朝机器人靠近），且落入机器人到下一 breadcrumb 的
   安全走廊时，才预测为碰撞风险。安全距离为机器人半径 0.28m + 行人约 0.30m + 0.15m margin。
4. 碰撞风险出现时，在当前行进方向两侧生成 NavMesh 候选，选择离未来行人更远且路径有效的一侧；
   无可行侧向点则原地等待。风险消失后继续原 observed trail。所有运动仍经过 NavMesh 投影。

`quality.json` 新增 `trail_follow_count`、`trail_follow_frames`、`trail_follow_source`、
`future_avoidance_count`、`future_avoidance_frames`。日志为
`target_path_follow_observed_trail` 和 `future_target_collision_avoidance`，不应再出现
`future_goto` 作为路径目标。下一轮 `run_info.txt` 必须为 `git_commit=bf13888...`，重点检查
`0001/ep1`、`0039/ep0` 是否沿人的实际拐角轨迹通过，以及人在回头靠近时是否提前侧向避让。

### 2026-07-17 12:15 复跑与修复（提交 `98e10b1`）

日志 `two_scenes_test_20260717_115823` 使用 `bf13888`、`max_steps=600`。机器人视觉隐藏正常：
四份 `quality.json` 均为 `hidden=true`，`hide_robot_visual_geometry()` 对机器人所有视觉几何调用
`MakeInvisible()`，相机和碰撞状态保留。

- `0001/ep0`：tracking 0.728，0 collision、仅 1 snap reject，但 future avoidance 持续 71 帧，
  视频中的环境边缘问题可能与长时间侧向避让有关。
- `0001/ep1`：tracking 1.0，但走廊处 3 次 projection cycle、连续 24 snap reject；0.28m
  footprint 对该脚本行人通道仍不可达，历史轨迹实际只使用 11 帧。
- `0039/ep0`：视频不明显，但指标是真实失败：tracking 0.364、final distance 4.07m、recovery 后
  `navmesh_collision`。
- `0039/ep1`：已正确 `char_done`，tracking 0.938、final distance 1.08m；reject 只因一次错误
  recovery。末尾机器人在两个相距约 1.8cm 的点间抖动，因为人已静止但旧 breadcrumb 仍驱动平移。

`98e10b1` 将统一抽象 `footprint_radius/planning_radius` 从 0.28m 降为 0.20m（相机高度不变）；
选中的 breadcrumb 会持续保持到机器人距离其 0.15m 内，不再只使用一帧；行人静止后立即停用
trail 和 future avoidance；stuck recovery 仅在机器人与真实目标距离超过 `safe_dis_max+0.05m`
时允许，避免安全距离内误 recovery。下一轮必须为 `git_commit=98e10b1...`，重点核对走廊通过率、
`0039/ep1` 是否无 recovery/末尾抖动，以及 `0001/ep0` 的 future avoidance frames 是否下降。

### 2026-07-17 14:50 复跑与修复（提交 `42569ae`）

日志 `two_scenes_test_20260717_143812` 使用 `98e10b1`、`max_steps=600`。结果已从四个 rejected
改善为两个 accepted：`0001/ep1` tracking 1.0、0 snap、259 trail frames，成功通过走廊并
`char_done`；`0039/ep1` tracking 0.840、0 snap/recovery，`char_done` accepted。

剩余问题：`0001/ep0` 在 step 65、位置约 `(-7.96,3.51)` 被 occupancy 连续判障碍，但全程
0 snap，说明 occupancy 与 NavMesh 边界不一致；`0039/ep0` 的 breadcrumb 有不可达点，导致
7 projection cycles、2 recoveries，step 199 `navmesh_collision`。此外 `0001/ep1` 虽 accepted，
行人停止后机器人仍在两个相距约 3cm 的点间高速往返，因为欧氏距离 1.12m 已安全，但绕障路径距离
较长仍驱动平移。

`42569ae` 的修复：行人静止且欧氏距离不超过 `safe_dis_min+0.05m` 时强制平移为 0；breadcrumb
只有从机器人当前位置确实存在 NavMesh path 时才可选择/保持；`robot_in_obstacle` 必须 occupancy
连续命中且机器人距离最近有效 NavMesh 点超过阈值才成立，物理碰撞检测仍独立保留。新增
`quality.json.metrics.occupancy_navmesh_disagreement_frames`。下一轮必须是
`git_commit=42569ae...`，重点确认 `0001/ep0` 不再早退、`0001/ep1` 终点无 3cm 往返、
`0039/ep0` 是否跳过不可达 breadcrumb 后继续全局规划。

### 无实体 policy camera carrier（提交 `5546619`）

此前 `hidden=true` 只代表机器人 Mesh 被 `MakeInvisible()`，Go2/Dingo/G1 articulation、关节、
碰撞体以及相机父子关系仍存在；物理更新、椅子接触或强制 root pose 同步仍可能造成卡顿和镜头晃动。
`5546619` 默认启用 `--camera_only_carrier`（环境变量 `CAMERA_ONLY_CARRIER=1`）：

- policy camera 改挂独立 `/World/PolicyCameraCarrier/front_cam`，不再挂 robot link/articulation root；
- carrier 位姿直接来自 canonical oracle 的 `_KIN_POS/_KIN_YAW`，每步刚性更新；
- 记录的 robot pose 也读取该虚拟 carrier 位姿，不读取 physics articulation；
- 兼容 robot USD 暂时仍加载以避免重写旧 reset 流程，但全部 CollisionAPI disabled、视觉 Mesh hidden，
  且 oracle 更新不再对它调用 `set_world_pose`/`set_joint_positions`；因此它不再是采集实体；
- camera base height/camera offset 继续使用所选 Go2/Dingo/G1 preset；NavMesh、0.20m occupancy footprint、
  observed trail 和未来碰撞避让仍约束虚拟相机，不能穿墙。

新 `quality.json.robot` 应显示 `camera_only_carrier=true`、
`physical_collisions_enabled=false`；`camera_info.json` 的 `camera_link` 应为
`/World/PolicyCameraCarrier`。下一轮必须使用 `git_commit=5546619...`，确认椅子不再通过物理实体卡镜头、
RGB 视角无 physics/joint 抖动，同时检查虚拟 NavMesh 跟踪质量。

**状态更新：`5546619` 已由 `2258002` 回退，不可作为当前运行说明。** 两次使用 `5546619` 的
运行 `two_scenes_test_20260717_153504`、`153703` 均约 1 分 45 秒返回 exit code 0，但没有生成任何
episode/quality；原始场景 `.log` 未被上传，因此无法可靠定位 traceback。当前恢复 `42569ae` 的稳定
robot-link 相机方案。提交 `a9e5cdb` 修改 `test_two_scenes.sh`：每个 scene 必须生成 2 个
`quality.json` 才能标记 OK，否则以 code 2 失败，并将日志最后 160 行保存为
`<scene_id>_error_summary.txt`。下一轮必须使用 `git_commit=a9e5cdb...`；若仍失败，上传该 summary
即可直接定位，不再根据空目录猜测。

`test_two_scenes.sh` 是严格串行：`SCENES=(0001 0039)` 的 `for` 循环内使用前台
`docker run | tee`，读取 `PIPESTATUS` 并完成产物检查后才进入下一 scene。一次运行产生两个 `.log`
是预期行为，分别记录两个顺序执行的场景，不表示并行。`38a64aa` 会在 `run_info.txt` 记录每个
scene 的 serial index、started/finished time、exit status、quality count、log path 和 log bytes；
`.log` 缺失或为空直接失败。仓库没有忽略 `test_runs/*.log`，上传日志时必须把两个场景 `.log`
一起 `git add`/push，不能只选择 quality/NPZ/run_info。失败还会额外生成
`<scene_id>_error_summary.txt`（完整 `.log` 仍保留）。
