# Phase 3 接管记录

2026-09-14，用户授权统一接管。完整产品目标继续以桌面接入说明为准：任意目标跟随、三模式导航、恢复搜索、密集动态避障和端侧验收。当前阶段先完成可验证的人物跟随闭环。

## 接管时实际状态与决策

原会话在接管前后完成 `next007_phase3_recovery_pilot_v1`：8个 train 恢复锚点、512 optimizer steps。其启动字段错误已被原会话修正。本次没有重复训练，也没有删除任何 checkpoint、数据或失败记录。

同一 STT val index 514 的50步诊断中，原Phase 2与pilot结果如下。这不是完整episode SR。

| 指标 | 原Phase 2 | 恢复pilot |
|---|---:|---:|
| following frame rate | 0.52 | 0.32 |
| 目标丢失 / 重获次数 | 1 / 0 | 2 / 1 |
| robot path / target path | 0.33729 | 0.20184 |
| human collision | 0 | 0 |
| 正式episode success | null | null |

结论：pilot不晋升为主模型，仍以 `32c8f2f73277cfab8c3c7c22a53994061f7178498cfa003e1635b9576641bc9c` 的Phase 2 checkpoint为后续修复起点。pilot训练集ADE虽降至0.002677m，但闭环总体退化。完整证据及SHA-256见 `outputs/takeover/DECISION.json`。

## 已安装和验证

- `omtrackvla/training/sequence_training.py`：真实政策序列前向，同时延续GRU、目标记忆与绑定状态，支持无梯度历史预热和TBPTT；4帧视觉历史不冒充4次政策调用。支持仅真实锚点有监督，干净回放保留停车、可见性、身份、bbox及ego监督。BCE显式使用FP32，修复autocast外BF16高正logit的梯度饱和。
- `omtrackvla/training/recovery_state.py`：供新trainer接入的运行目录保护、原子保存、代码/配置/数据/world size/runtime校验、各rank RNG/采样状态恢复与验证集模型选择。模块已测试，尚不声称旧v1 trainer已接入全部功能。
- `scripts/preflight_phase3_sequence.py`：官方DA3与真实Phase 2 checkpoint、SAGE3D train四模式、完整两政策步前向和反向。第一次因BF16可见性梯度失败的报告保留；FP32修复后7个检查全部通过。GRU `weight_hh` 的waypoint-only梯度范数为0.0010521。0 optimizer step，不是正式训练结果。
- 原v1 runner加入互斥锁与完成即退出；tmux launcher在任何日志写入前检查完成状态。原文件备份于 `.codex_upload/takeover_before_orchestration/`。实际调用两入口后，旧日志与完成记录SHA-256不变。
- 7项序列测试、14项恢复与文件保护测试在远端正确Python下全部通过，含Torch CPU AdamW/Dropout中断恢复一致性。环境没有pytest，因此使用unittest执行。

## 标签时间准入

`outputs/takeover/control_clock_stt0/measurement.json` 的STT train index0九步实测：每环境步world time增加0.048秒。配置为ctrl_freq=40、ac_freq_ratio=4；四次internal_step总计0.032秒，任务动作路径另有直接step_physics。不能仅用ctrl_freq或ac_freq_ratio推导RGB政策时钟。

旧expert脚本把 `[0,3,...,21]` 步写作除以30的秒数，此假设被实测推翻。若上述步长保持，21步是1.008秒而不是0.7秒。此结论仅针对已测配置/片段，不能把0.048硬编码为全局常数。旧报告保留，但旧恢复标签不准入v2正式训练。

新候选采集必须保存：reset到锚点的全部真实政策观测、逐步世界时间、RGB采集时间、标签侧机器人/目标姿态与动作、配置/代码/源rollout哈希。专家未来分支只提供监督，禁止写入GRU观测历史；按实测expert时间轴重采样到 `[0,.1,...,.7]` 秒，范围不足拒绝外推。

## 下一准入门

新候选入口 `scripts/probe_next007_recovery_sequence_v2.py` 与 `scripts/recovery_sequence_export_v2.py` 已在STT train index0真实运行通过。产物为 `outputs/takeover/recovery_sequence_stt0_v2/{report.json,sample/sample.json}`，保存0～9共10次政策调用的完整历史；模型输入不含专家未来RGB。实际21步专家分支覆盖1.024秒，中间出现0.056秒步长，因此不是一个可以统一除以固定频率的序列。新8点标签按实际时间重采样至0～0.7秒，未外推，并如实标注EVT-Bench train来源。sample SHA-256为 `b391fe2045cab67ee1f69b45f168f1d35b1c3b11dee54fca3e942fc0a3cd3ea6`。

5项候选导出回归测试亦在远端通过，连同前述21项共26项。该候选明确 `formal_training_eligible=false`，不继承旧样本准入；正式时间/场景隔离/安全质量准入仍待完成。

先验证新候选导出及时间/坐标/输入边界，再按物理场景隔离训练与恢复开发集。8个旧锚点中三个任务共享同一场景，不能按task拆分就宣称无泄漏。正式v2还需接入完整序列loader、恢复状态模块、独立验证选择和退出门，随后才启动新的优化器训练。

已验证的基础模块与候选数据管道不代表SR、碰撞率、主动搜索或Thor时延达标；这些仍需按原需求完成。

## 2026-09-14：实际时间数据准入与序列训练接通

已完成7条新采集，复用已验证的STT0，共8条schema2候选、6个物理场景。所有源产物绑定原Phase2父权重，采集前核验44个文件；全量结果保存在 `outputs/takeover/recovery_sequence_batch8_measured_fullprefix_v2/COLLECTION_RESULT.json`。每条保留reset到anchor9的10次真实调用，按实际世界时间重采样到0..0.7秒。

新增 `omtrackvla/data/recovery_sequence.py` 独立校验来源、媒体、因果历史、时间插值、坐标投影和实际0.7秒轨迹质量。旧整段专家轨迹通过不能代替实际标签通过；修复schema1原始样本无source.task的兼容问题，任务身份仍严格由完整旧sample_id校验。摄像机/碰撞记录仍是采集时的审计证据，不声称重新执行了独立物理验证。

冻结开发manifest：`outputs/takeover/recovery_pilot_scene_split_v2/manifest.json`，SHA-256 `9268ac7bf53a8392ab58a05112e8bd4db662889fa560708973e8afb807ca1e4e`。4个train场景对应4样本，2个val场景对应4样本，两侧均包含STT/DT/AT；同物理场景不跨分区。候选继续保持formal_training_eligible=false，只有独立manifest授予development_pilot角色。后续扩样本必须引用此manifest保留永久验证场景。

真实序列preflight发现并修复混合精度缓存问题：在同一个autocast上下文中，no_grad的6次历史预热会留下不带梯度的权重cast，后4次学习复用后导致GRU梯度为0。中央sequence helper在burn-in进入学习前清理缓存；真实BF16在缓存开启时8项检查通过。FP32、BF16关闭缓存对照也通过。新的CPU回归在旧代码上复现2个失败、修复后3项全通过。此前失败报告保留，不覆盖。

第一步优化器烟测又发现BF16归一化令ego cosine略大于1、损失为负；已将该损失改用FP32并约束cosine范围，新增回归验证非负且保留误差梯度。旧1步烟测 `outputs/training/recovery_sequence_development_pilot_v2` 保留，不在源码合同已变更后续训。更新后独立新目录为 `outputs/training/recovery_sequence_development_pilot_v2b`。

截至此处，远端92项不同的相关CPU测试均已通过，含序列8、autocast3、loader29、场景manifest19、trainer14、恢复状态14、导出5。真实候选8项反向检查及完整数据准入亦通过。

新版trainer `scripts/train_recovery_sequence_v2.py` 使用单GPU7、64步预算、clean四条件完整两决策序列与1条恢复序列，clean/recovery权重1/.25，policy和GRU学习率1e-5，adapter1e-6。验证使用固定16条SAGE val和4条独立场景恢复val，只有恢复ADE优于父模型且各clean模式/辅助项未退化才选新best；不自动晋升部署。保存模型、优化器、调度器、随机数和数据采样状态，绑定实际源码、数据、配置及runtime，禁止覆盖已完成run。

v2b的第1步已正常完成：adapter/GRU/policy均有非零有限梯度，ego损失非负；已按预设停止并从该last checkpoint恢复，仍保持总预算64。这里验证的是实际恢复执行，不等于已证明CUDA逐位复现；SAGE原JPEG依赖源目录不变，已有索引与sidecar链校验并不等于逐图冻结。

8个anchor9只是训练管道开发集，尚不覆盖实际丢失后恢复。后续仍须采集多个访问时刻，补遮挡/丢失/重获数据，进行固定完整episode闭环评测，再推进原始三模式导航、主动搜索、任意目标和Thor实机验收。
### v2b完成与决策

64步已完成，完成清单中的所有产物SHA-256逐项核验一致。恢复开发val ADE由0.295181m降至0.260211m（下降约11.85%），但safe_stop的预测轨迹ADE由0.008420m升到0.010848m，超过预设0.008941m回归门。因此4次验证均未选新best，部署主模型保持原Phase2。已逐张量确认保存的best模型参数与原Phase2完全相同。此处safe_stop ADE是离线预测偏移，不是实机移动距离或碰撞率。

证据：`outputs/takeover/RECOVERY_PILOT_V2_DECISION.json`。第一步的真实中断恢复执行已成功，总预算仍64；没有声称CUDA逐位复现。当前训练已结束。

据此固定下一轮“增强safe_stop监督且保留当前验证门”的假设，并规划增加丢失前后、遮挡与重获锚点；不因本轮结果降低验收阈值。其执行结果与数据计划状态见下文。现有数据与流程仅完成开发管道里程碑，完整项目仍按原始范围推进。用户配合事项见 `docs/project_completion_plan_zh.md`。

### v3完成：停车监督乘8，仍未通过原验证门

`outputs/training/recovery_sequence_safe_stop_v3` 已完成64步，决策为 `complete_not_promoted`。相对v2b，本轮训练配置仅新增 `safe_stop_waypoint_multiplier=8`；父权重、基础模型、种子、学习率、clean/recovery权重、预算、场景分区和验证阈值保持一致。基线完整JSON相同，64步的clean索引与recovery索引逐步一致，支持这次受控开发比较。

| 离线开发验证指标 | 原Phase 2基线 | v2b第64步 | v3第64步 |
|---|---:|---:|---:|
| 恢复开发val ADE（m） | 0.295181 | 0.260211 | 0.260643 |
| safe_stop预测轨迹ADE（m） | 0.008420 | 0.010848 | 0.009220 |
| 原clean验证门 | 基线 | 未通过 | 未通过 |

safe_stop上限仍为 `0.008420215453952551 × 1.05 + 0.0001 = 0.008941226226650178 m`。v3相对v2b减轻了停车项退化，但未达到门槛。第16、32、48、64步均只因 `per_mode.safe_stop.ade_m` 未通过，均未选新best。`TRAINING_COMPLETE.json`、`run_state.json` 与四次验证一致：best保持step 0、来源 `unchanged_phase2_parent`，不晋升v3、不启动正式训练，也未使用test_locked。没有降低阈值来接受结果。

本节于2026-09-14 11:12（北京时间）只读核查远端：完成/状态清单及best版本文件合计6项实际SHA-256全部相符；另核验原Phase2文件及永久场景manifest。配置、代码哈希映射、数据哈希映射和runtime元数据的规范化哈希与run合同一致，重新计算四次clean gate与报告完全一致。此核查未加载Torch或模型；v3仅确认step 0选择记录与文件哈希，未另行逐张量比较v3 best与原Phase2，不能把该核查写成张量相等验证。

证据已下载至桌面 `OmTrackVLA_project_audit/evidence_v3/`，包含v2b/v3原始JSON、配置、相关源码及独立核查报告 `V3_EVIDENCE_VERIFICATION.json`。归档 `OmTrackVLA_project_audit/takeover_v3_evidence.tar.gz` 共156个文件、177242字节，SHA-256为 `8b1ff2f39bf69e9cdd093a07987a0e26138ef72fb7ea5a41ac8fe47a9a01fc0c`。归档和其中155个原始文件下载后逐项校验通过；checkpoint仅留远端哈希，不下载权重。正式决策源为 `outputs/takeover/RECOVERY_PILOT_V3_DECISION.json`。

### 丢失恢复数据缺口与长序列计划状态

当前clean `safe_stop`评测是冷启动无目标条件下的离线停车保留，不能等同于“已初始化跟随目标、随后视觉/UWB失效”的状态转移，更不能证明主动搜索、重获、实机位移或碰撞率。原始目标仍包含丢失后的主动搜索和重获；停车项是局部回归约束，不构成缩减原需求的理由。

已只读盘点旧8条源rollout：每条仅12步，四条train源全程GT可见；只有永久val的DT1700第7步、STT1700第6步出现GT不可见，均无重获。因此从旧前缀增加锚点无法补出train丢失恢复事件。GT panoptic mask为空尚不能区分遮挡与离开视野；模型低置信度也不能替代GT。动作后第k帧的GT须与决策k+1对齐。

长序列采集计划已冻结在桌面 `OmTrackVLA_project_audit/multianchor_review/train4_long_rollout_plan.json`：STT0先做64步计时探针，再对永久train的AT401/episode9、DT200/episode7、STT0/episode4、STT3100/episode2各采128步。计划绑定原Phase2、原场景与初始化，scan=1、wait=0，不跳过失败初始化；无丢失结果亦须保留，不将val场景转为train。计划文件SHA-256为 `cb3e63eb76bd1a2663369accb9a5fff7823b3d23cda0a54bd30a9321cbf289f0`。

在上述11:12核查时，长序列仍处于性能profiling阶段，未完成四条128步采集。STT0探针约19.2秒/步耗在 `env.step`，与无政策clock/replay约0.7秒/步存在差异；当时政策进程及CPU线程影响只是待验证假设。后续实测及对该假设的修正如下。计时探针或冷启动停车实验不计作丢失恢复训练或验收完成；实际标签仍使用观测到的世界时间，不能由配置频率推定。

### 性能实测、设备混杂修正与长序列启动

默认OpenCV线程数192与线程数1的两条真实4步profile均完成；另完成32步不带profile的对照。GPU2默认线程慢run的前32步与GPU3线程1快run比较，`env.step`中位数分别为19189.3764395 ms和44.3562105 ms。已独立从原始逐步JSON重算这两个中位数，并核验两个源产物SHA-256。原64步慢探针在保存50步后SIGABRT（exit -6），其部分结果和失败状态保留，不能写成完整64步结果。

32步的全部policy字段（除inference_ms）、GT可见性/距离/碰撞、相机姿态及政策强制渲染RGB统计一致。但 `env.step`返回的图像从旧帧变为新帧，`human_following`在第5、10步不同，涉及旧panoptic/facing度量链。因此保留原始整体比较的failed记录及后续语义审计，不宣称所有诊断或评测指标等价；旧following_rate须重新审计后才可比较。这是开发仿真对照，不是Thor时延或产品验收证据。

最初据此增加 `OPENCV_FOR_THREADS_NUM=1` 的计划为 `.codex_upload/train4_long_v1/train4_long_rollout_plan_threads1_v2.json`，文件SHA-256 `46fae01d2102a90a16c3c7ef066ee70b509597abacbf4a95f8b933028190f29b`。四条固定train均按该计划启动；STT0在GPU3正常运行，但GPU1/2/4上的另外三条仍慢。最初对照同时改变了GPU和线程数，存在混杂，不能把整个加速归因于OpenCV。

接管执行方随后交叉核查：GPU3默认线程的4步 `env.step`仍为44～48 ms，GPU5线程1首步仍约32秒；全卡nvidia-smi查询阻塞，而只查询GPU3可立即返回。现有证据指向设备相关差异，尚未确定具体机制，也未证明GPU映射错误。保留旧线程计划和原归因记录，后续采用修正后的设备结论。

GPU3上的STT0请求128步，实际84步因 `episode_over`自然结束（exit 0）；记录1次目标丢失、0次重获、success=0。该源结果保留，不能记成采满128步。另三条慢源由接管执行方SIGTERM停止，保留 `interruption_request`、日志及部分结果，再按相同场景/index/episode/初始化/Phase2改由GPU3串行，在后缀 `_gpu3_v2` 的新输出目录执行。新计划 `.codex_upload/train4_long_v1/train4_long_rollout_plan_gpu3_v3.json` 文件SHA-256为 `9db8868490703c8d1bc1449b3e63d3452e9f766432aaad4c6965a254f09fa9a0`，规范化plan ID为 `1ddb6ccae03143d25ab4f79a7b14837fcd7e401345dda91d8633dcb1588b5618`。这里仅记录调整及启动阶段，最终完成情况另行核查；不声称四条长序列或恢复训练已完成。

32步对照、profile、原线程修订计划和11:21:37（北京时间）的运行快照另存桌面 `OmTrackVLA_project_audit/takeover_timing32_longrun_snapshot_evidence.tar.gz`，共63个文件、1585984字节，SHA-256 `7743180af615638a195450bf937aab8d3e0b30f54eee102f664c9a85c28d285b`。已安全解包至 `evidence_timing32_longrun_snapshot/` 并逐文件核验；其快照里的“cross-over待做”是当时状态，已由本节后续设备交叉核查修正。GPU3新计划原始字节另补在该目录的 `.codex_upload/train4_long_v1/` 下，未改动该快照归档或v3归档。详细32步证据源为 `outputs/takeover/ENV_STEP_THREAD_CONTROL_32STEP_SEMANTIC_AUDIT.json`。

### 四条长源episode已自然结束，尚无重获样本

随后已只读读取接管汇总 `outputs/takeover/TRAIN4_LONG_ROLLOUT_RESULT.json`：四个固定train源分别实际运行AT401 85步、DT200 104步、STT0 84步、STT3100 93步，共366步，均在请求的128步之前自然 `episode_over`。三个源出现GT丢失，零个源出现重获；不更换episode，也未安排人工延长。它们是完整自然episode的模型访问源，仍为 `formal_training_eligible=false`，尚待锚点冻结、专家重标注及数据准入，不能宣称已补齐重获训练或完成项目验收。

设备交叉试验最终决策为 `outputs/takeover/ENV_STEP_DEVICE_CROSSOVER_DECISION.json`：GPU3默认线程也快，GPU5线程1的完整4步仍慢（预热后env.step中位43.506秒），明确否定仅线程数导致此前差异的解释；实际渲染设备/驱动原因未定。两份最终JSON的原始字节已另补至 `evidence_timing32_longrun_snapshot/outputs/takeover/`，SHA-256分别为 `53bd602842d5c73cce1a03e8e7ef7b84c7e98b835d53a57831d5bba7e2bd9039` 与 `9fbca196d326afa4676bc2244816d9b48f8a2c0075161795dfd733d4f4884b63`，原时间快照和v3归档保持不变。

## 本轮闭环验证与 LightNav 参考结论

本轮结果：**v4 不晋升为主模型；继续从原 Phase 2 推进感知监督和搜索控制。** 原验收范围仍为任意目标跟随、图像/坐标/混合三模式导航、主动搜索和重获、避障及 Thor 离线部署，尚未完成项目验收。

用户随后明确暂无实机硬件，当前只关注仿真并争取 SOTA。因此实机接入和 Thor 测量暂停，近期工作以 EVT-Bench 仿真为主：核对 TrackVLA、LightNav-0 等公开基线的任务条件、数据分割及计分协议，先建立可比基线，再验证感知、恢复搜索和训练改动。四例开发诊断或离线 ADE 改善均不能作为 SOTA 证据；原目标条件和部署输入边界继续保留。

## 已完成的训练与数据工作

四条长训练源自然结束，共 366 个动作步骤。按采集前冻结的 21 个锚点收集专家标签，19 条通过、2 条因 episode 提前结束失败；失败记录全部保留，没有替换样本。独立准入后共 23 条 train、4 条原有 val，物理场景隔离不变，val 路径与哈希不变。

v4 使用完整 reset→anchor 历史，逐次延续 GRU 与目标记忆，在真实末 4 次调用学习，不用填充凑序列。64 步训练完成；按原离线规则选中第 16 步，恢复 ADE 从 0.295181 m 降至 0.286103 m，约改善 3.1%。第 32/48/64 步未通过正常模式保留约束。冷启动 safe-stop 的预测轨迹误差不是执行位移，也不能代替已初始化后丢失的恢复能力。

## 固定四例闭环对比

在任何本轮闭环输出产生前固定三组协议：原 Phase 2、离线 best16、诊断 last64；每组同四例、同初始化、同配置和 runner，GPU 3 串行。四例实际分别运行 90、46、45、44 步，均自然结束；每组共 225 步，三组共 675 步。12 次运行均正常退出，没有漏计失败或换场景。

| 开发集结果 | 原 Phase 2 | v4 best16 | v4 last64（诊断） |
|---|---:|---:|---:|
| 成功 episode | 0/4 | 0/4 | 0/4 |
| 满足跟随判据的帧 | 21/225 | 22/225 | 20/225 |
| GT 目标丢失次数 | 5 | 5 | 5 |
| GT 再次可见次数 | 1 | 1 | 1 |
| 因视觉不确定停车的步骤 | 186 | 185 | 184 |
| 已检测到的旧返回帧 | 0 | 0 | 0 |

best16 只在 AT1700 多 1 个跟随帧，没有新增成功案例或 GT 再次可见事件。GT 再次可见不代表控制器确认了同一身份，更不代表主动搜索成功。各例 `human_collision` 记录为 0，也不能替代分别统计静态/动态碰撞的原验收要求。四例来自两个永久验证场景，属于开发诊断，不能估计完整项目的最终 SR。

因此继续增加单一恢复 waypoint 训练量没有得到闭环收益的支持。当前控制器在视觉置信不足且 UWB 缺失时将三轴动作清零；现恢复样本也没有可见性、可信 bbox 或候选身份监督。应同时补齐感知标签和状态控制，并分别验证“只改模型”“只改控制器”“二者一起改”的效果。

原始证据：`evidence_val4_v1/outputs/takeover/permanent_val4_closed_loop_v1/`。下载包 `takeover_val4_evidence_v1.tar.gz` 的 SHA-256 为 `c28cb50916c707367eb337cd9d237bd51ab9aa2c5bd17e47e8615d4ad9888a48`，134 个文件；其中清单所列 133 个文件均已逐项校验。图片、视频和权重留在远端，远端套件清单记录其哈希。

## LightNav-0 的采用方式

已审查固定 commit `c6f40e3220edbf7011e4f17eaf2c865416737d4d` 的 205 个文本文件，详见 [LightNav 审查](lightnav_reference/LIGHTNAV_REVIEW.md)。

- 借鉴请求序号与任务隔离、采集时刻里程计对齐、过期响应拒绝、Thor 离线环境配置及视觉缓存设计。
- 它的语言条件、10 个 SE(2) 空间路点，与我们指定目标/坐标条件和 8 个带秒偏移的 xy 路点不同，不能直接替换接口。
- 原库 MPC 主要做运动学轨迹跟踪，缺少通用障碍物约束，也缺少完整的轨迹采集年龄限制；需补强后再采用相关设计。
- 作者的 Thor 178 ms 是服务端中位数，不能作为我们完整采集到控制链路 ≤200 ms 的证明。
- 根代码 Apache-2.0 不覆盖所有适配代码、资产和权重；当前只读参考，没有导入不明许可的权重或第三方评测代码。

## 正在补齐的下一层

1. 独立搜索状态机已安装到 `omtrackvla/control/active_search_fsm.py`，实现原目标任务边界、输入时效、确认滞回、累计搜索预算和有期限的零平移偏航建议。审查又修正旧许可回放、处理延迟凑满确认时长、重复帧刷新目标记忆等边界；本机及远端 Python 3.9 均通过 50 项 CPU 测试。它仍需真实身份/运动许可来源和执行端 watchdog，不能称为已部署的主动搜索。
2. 已在固定四条 train 长源重放同一动作，采集 370 个同次渲染 RGB/panoptic 观测。独立验证器重新读取 740 个媒体文件、复算标签及 4,348 项对齐检查，全部通过；19 个原样本保持不变。645 次前缀比较包含重复，去重只有 228 个历史观测具有原像素和时间参照；其余 138 个非终末观测只核对原策略 RGB 统计及几何等，4 个终末观测没有下一策略 RGB 统计。不能把 370 帧全部称为原图逐像素复现。
3. 标签 loader `omtrackvla/data/perception_sidecar.py` 与独立 bbox/visibility 有效位已安装到远端。完整 19 条真实前缀均通过输入不变、标签对齐及原锚点轨迹监督检查；新标签只用于末四次真实调用，未知身份、停车、运动许可和绑定标签仍无效。相关 65 项不同的 CPU 测试通过：首次旧 v4 测试因 fixture 环境路径报错，设置已有 `OMTRACKVLA_REPOSITORY` 后仅重跑受影响模块通过，失败日志保留。采集器 47 项及独立验证器 19 项测试是另外的历史检查，未混入此 65 项计数。
4. 已用原 Phase2 权重在 GPU7 做真实 BF16 前向与反向：STT anchor027 完整 28 次调用、24 次预热、末四次标签 `[1,1,1,0]`，三个有效目标框。visibility 和 bbox 损失分别为 1.51743054、0.06158895，两个头均有有限非零梯度。没有构造优化器、更新或保存权重；父 checkpoint 哈希不变，loader 的显式 optimizer 入口仍拒绝使用。这证明监督接线有效，不代表定位精度或闭环成功率已经提高。CPU 和反向证据分别在 `perception_supervision_review/cpu.json`、`backward.json`。
5. 永久四例感知评估协议已冻结，分母为 229 个标签观测、225 次非终末预测；缺失/非法预测计入失败，不按预测置信度筛掉错误框。21 项 CPU 协议/指标/模拟回放测试通过，真实 val 采集与独立准入仍在实现；既有带绘制叠加的图像不能当作原始输入。补齐该验证数据后才开展感知优化器实验，再接身份校准与有界搜索仿真对照。不能把 FSM 的 rad/s 直接当成 Habitat 归一化 yaw；实测世界时间间隔有 0.048 和 0.056 秒，需要明确命令时间合同。三模式、非人物目标与场景规模继续保留，实机部分按用户要求暂停。

当前输入链接下的场景资产清单为 HM3D train 800、HM3D val 100、MP3D 90，共 990 条不同规范化名称；几何 GLB 文件头及非空导航网格存在性全部通过。此处未加载场景、未证明跨数据集物理地点去重和 Real2Sim 来源许可，也未搜索其他存储根，因此不是 990 个通过正式验收的环境。清单为 `scene_asset_inventory_summary_v1.json`，完整远端记录为 `outputs/takeover/scene_asset_inventory_v1/manifest.json`。

训练源诊断中，366 次决策的可见性 TP/TN/FP/FN 为 121/202/0/43。与独立核验标签对齐后，164 个 GT 可见且框有效的决策中，预测框平均 IoU 为 0.311749，IoU≥0.5 仅 45/164。43 个低置信停车决策中，32 个预测框与目标完全不重叠，平均 IoU 仅 0.072700；STT3100 的低置信 13 帧全部零重叠。因此不能简单放宽停车阈值，应先修定位和身份保持。定量报告见 `perception_label_review/bbox_localization_diagnostic.md`，实际输入对照图为同目录的 `visibility_bbox_diagnostic.png`。

搜索转向单位审计已完成：当前 Habitat 动作实现每次积分的转角约为 `0.157 × normalized_yaw`，366 个原动作最大转角残差约 1.16×10⁻⁶ rad。实际世界时间增量有 337 次约 0.048 秒和 29 次约 0.056 秒，不能把 FSM 的 rad/s 直接传给动作接口，也不能硬编码世界步长。下一步须定义命令作用时间，再实现转换及实测；这次只核对既有动作，未启动原地搜索。详见 `active_search_implementation/review/SIMULATOR_YAW_CONTRACT.md`。

当前无需用户手动启动训练或补标注，也不需要提供硬件信息。按用户最新要求，先推进仿真模型和可比基准实验；实机条件留待后续恢复该阶段时处理。

## 仿真阶段进展（2026-09-14 14:08）

当前继续以仿真 SOTA 为目标，暂无 SOTA 成绩。实机适配暂停，不需要用户提供硬件。原项目的任意目标、三种目标输入/导航模式与丢失主动搜索要求继续保留。

**已完成原模型的共同原始图像基线，主要瓶颈是定位。** 四条永久内部验证轨迹的 229 帧观测、458 个媒体文件经过独立验收，2,274 项对齐检查通过；在相同 raw 图像历史上完成 225 次真实 GPU7 推理。48 帧 GT 可见图像中平均框 IoU 0.253，仅 4 帧达到 IoU≥0.5，只有 1 帧同时达到当前 0.90 置信度阈值。可见性 TP/TN/FP/FN 为 33/171/6/15。没有优化器步骤或新 checkpoint，原权重未变；这次是感知诊断，不是新的闭环提升。

评测复用真实部署控制器的 OpenCV 预处理、完整因果历史与逐例状态重置，后续真值不进入模型，终末帧不推理，失败不丢弃。21 项新增 CPU 测试在本机和远端通过；首轮远端 fixture 路径失败已修复并保留日志。原始旧视频没有完整未绘制像素和 worldtime 参照，不声称新数据与旧视频逐像素/时钟完全重现。这四例无可见干扰人，不能证明身份抗干扰能力。

**正式参评模型需要重新建立训练来源链。** 当前 Phase2 的 Phase1 祖先所引用训练清单与官方验证集有 9 个场景标识重合；缺少逐样本训练记录，尚不能确定它们实际参与训练的数量。SAGE 的重命名场景还没有可验证 canonical 映射。现模型保留用于内部开发与回归，不能声称其正式评测已排除数据泄漏。新训练路线为官方 DA3 骨干、重新初始化任务参数、仅采用场景来源可核验的官方 train。

已生成并独立校验第一批 **48 episode / 48 个不同场景标识**的采集候选清单，STT/DT/AT 各 16，分别含 12 HM3D 和 4 MP3D。排除了全部 101 个官方验证场景以及 6 个永久开发场景，不按模型成败挑样本。清单只是元数据资格，后续仍需完成教师轨迹采集、媒体/时钟验收和训练数据加载；尚未开始这批优化器训练。

**LightNav 已建好独立基础环境，完整依赖安装正在推进。** 固定源码 `c6f40e3…` 在 Linux 上完成完整校验，Python 3.11.16 与 pip 26.2.1 可用；旧环境不变。完整依赖锁为 181 个固定 wheel、约 4.82 GiB。远端 DNS 和直连 HTTPS 均失败，正通过本机下载、中转后离线安装。仅在独立目录操作，GPU0 的 FLUX 服务保持受保护。模型权重与 GPU 推理尚未完成，因此不能称 LightNav 基线已跑通。

**完整基准清单已固定。** STT/DT/AT 各 1,405 集，合计 4,215 集；30 分片覆盖全部 episode。101 个场景几何/navmesh 文件均存在，但逐场景加载和 humanoid 适配仍待验证。公开参考长度只匹配 1,353/1,405 集，缺失 52 项已明确列出：完整 SR 保留 1,405 集；TR 同时披露上游缺失回退规则和共同 1,353 集子集结果，不能静默补值或漏集。

接下来完成跨场景教师轨迹试采、目标定位与可见性训练，并用共同 raw 评测和独立闭环验证候选；同时完成 LightNav 依赖、固定权重和共同仿真接口。SOTA 结论必须来自完整、同条件比较。

详细证据位于 `perception_validation_review/inference_v1/BASELINE_RESULT.md`、`simulation_sota_review/PHASE2_DATA_PROVENANCE.md`、`simulation_sota_review/FULL_BENCHMARK_REFERENCE_ADDENDUM.md`，以及 `simulation_sota_review/lightnav_stage_a/`。

## 仿真阶段进展（2026-09-14 15:20）

目标仍是仿真 SOTA，目前尚无 SOTA 或新闭环跟随成绩。无需用户提供硬件。原项目的任意目标、三种目标输入/导航模式、丢失主动搜索与同目标重获范围保留。

**清洁来源的第一轮感知训练和四场景诊断已完成。** 新图从固定官方 DA3-SMALL 通用骨干开始，冻结全部骨干，重新初始化感知任务层，不加载旧任务 checkpoint。首先完成32项独立空间读出CPU测试、真实sidecar接线、12项RGB图集成测试和真实GPU7 BF16反向；其后新建仅限目标框/可见性的训练准入，在一个已核验的官方train场景上比较三个读出方案，每组256步、batch 8、相同采样顺序，共768次优化器更新。

训练使用观测1–122，排除初始模板帧与终末帧；首次RGB/框及四帧因果RGB历史是唯一模型输入。原始图像、标签、时间及259个封存文件已独立重算并绑定哈希。该轨迹动作教师不合格，未用于动作、路径点、停车、绑定或身份监督。旧证据中的optimizer=false保持不变，新准入仅用于本次感知拟合诊断。

| 方案 | 训练场景框IoU（41帧，排除初始化） | 四个永久开发验证场景框IoU（48帧） | 验证IoU≥0.5 | 验证置信度≥0.9且IoU≥0.5 |
|---|---:|---:|---:|---:|
| 原式pooled + SmoothL1 | 0.699 | 0.143 | 2/48 | 2/48 |
| pooled + L1/GIoU | 0.690 | 0.145 | 1/48 | 1/48 |
| 新空间读出 + L1/GIoU/空间分类 | 0.732 | 0.341 | 15/48 | 6/48 |

三组各完成225次验证推理，无缺失预测或输入绑定失败，没有执行候选动作。新空间读出在这次开发诊断的框定位上领先；其0.9可见性阈值仍有25/48漏检，不能据此放开运动或宣称全面性能领先。独立拟合复核通过7,573项检查，738条before/after预测和768条采样记录均复算一致。单场景训练、四场景开发诊断不足以支持SOTA、跨人物身份鲁棒性或产品验收。

旧Phase2仍保留为工作回归基线。其共同raw诊断IoU为0.253、IoU≥0.5为4/48；祖先训练清单与官方val有9个场景标识重合，缺少历史逐样本ledger，不能认证干净来源。该旧模型与新三组训练预算、模型图不同，上述数字不构成严格同预算架构消融。正式候选沿用官方通用骨干 + 可核验官方train的新来源链。

**动作教师仍需修复，已定位运行时网格与滑移问题。** 固定第一case完成V1/V2两次真实采集，各123动作、124观测，目标实际逐帧位置与世界时钟完全一致。V1跟随19/123，触发距离<0.5m的锁存代理；V2修复坐标教师误用视觉优先分支，并调整试采平移上限，过近代理和多代理接触指标均为0，但跟随只有10/123，仍未合格。二者均由目标完成路径后的等待计数自然结束，不是成功终止。

V2后半程路径查询持续失败，进一步定位到真实`data/scene_datasets/navmeshes/zR6kPe1PsyS.navmesh`：机器人进入另一个不连通分量。相邻`.basis.navmesh`的离线查询全部可达，不能用其代替实际运行网格；只投影路径起点也不能修复。实际缓存不在V1/V2原冻结资产清单中，当前缓存哈希是采集后测量，不能追认为此前已完整固定的运行资产。非圆柱底盘的偏移点滑移会在转向时移动底盘中心。下一版只在教师侧增加动作预测与可达分量检查，并绑定真实网格；不替换网格、不改变目标速度，不声称部署主动搜索已完成。

**48个跨场景训练候选已完成基础资产预检。** 三任务各16，48个不同canonical场景，排除全部官方101验证场景与6个永久开发场景。48组episode、邻接geometry/navmesh和动态avatar目录均存在，430个文件约2.81GB经过哈希检查；这是文件级资格，实际运行缓存网格正在单独预检，尚未完成48条采集或跨场景训练。

**LightNav独立依赖安装仍在后台推进。** 固定源码和Python3.11环境已建，181个wheel约4.82GiB通过本机中转离线安装。已有大型wheel完成远端整文件SHA核验，连接重置后按已校验偏移续传；不重复启动。主权重约9.70GB尚未传输，LightNav GPU推理与正式共同基线尚未跑通。GPU0的FLUX保持受保护，Habitat仍只在GPU3串行运行。

完整EVT清单已固定为三任务各1,405集，共4,215集。公开参考长度只匹配1,353/1,405集，52个缺项及TR回退规则已披露。后续须完成实际运行网格、逐集avatar与共同输入条件适配，再进行完整同条件比较。

下一步：完成教师动作预测检查并在同一case实测；开展固定48场景的原始感知采集与训练；在相同预算下保留原式读出对照，并以感知验证和实际闭环共同决定候选。检查点均保存为独立实验产物，尚无模型晋升。

主要证据：`perception_validation_review/clean_perception_fit_v1/`、`clean_perception_fit_independent_review_v1/`、`clean_fit_val4_v1/`；`simulation_sota_review/clean_teacher_pilot_v1/`、`clean_teacher_pilot_v2/`、`clean48_asset_inventory_v1.json`。

## 仿真阶段进展（2026-09-14 16:17）

仿真工作正在继续，目前尚未达到 SOTA。无需提供硬件。原项目的任意目标、三种目标输入/导航模式、丢失后主动搜索与同目标重获范围保留；当前 EVT 人物实验只覆盖其中一部分。

**固定 48 场景的真实原始数据采集已完成。** STT、DT、AT 各 16 个不同场景，共保存 3,003 帧。48 次尝试全部封存，全部工作进程回收；GPU0 FLUX 在每次运行前后的检查中均存在。Habitat 全程只在 GPU3 串行运行，没有替换失败场景。

| 任务 | 固定候选 | 保存观测 | 自然终止且有后续帧的轨迹 |
|---|---:|---:|---:|
| STT | 16 | 938 | 12 |
| DT | 16 | 996 | 11 |
| AT | 16 | 1,069 | 11 |
| 合计 | 48 | 3,003 | 34 |

另外 13 个场景在初始状态的动作检查处停止，只有初始化帧；1 个场景缺少有效初始化。这 14 个成员保留在完整台账中，没有变成训练样本。34 条完整轨迹提供 2,921 个非初始化、非终末训练观测，含 2,644 个可见目标框；评分集合含 2,955 个非终末观测，初始化另报。

34 条可用轨迹的独立校验记录 6,454 个文件、约 2.90 GB 源字节和 2,989 个原始观测；连同 13 个只有初始化帧的场景，47 个可验证成员合计 6,662 个文件、约 2.92 GB 和 3,002 个观测。缺少初始化的另一帧保留在 48 次尝试记录中。逐文件哈希、时钟、帧序、首次框、原始 panoptic 目标标签均经过检查。原 48 场景仍是固定候选集，实际可训练场景为 34，不能写成 48 场景均已通过。新建感知限定准入只授权目标框和可见性拟合；教师动作、路径点、停车、绑定、身份标签均未获训练准入，旧证据中的标志不变。

**机器人滑入不连通区域的问题已在固定试例中修复。** V3 教师沿用 V2 的场景、目标、速度、随机种子和真实运行网格，在执行前预测底盘平移与转向滑移，并拒绝不可导航或无法到达当前目标的落点。全部 124 个实际目标位置、世界时刻与 V2 相同；V3 机器人全程可导航、到当前目标的路径全程存在。

独立 CPU 复算了 3,218 个候选与最终选择，实际干预 11 次。123 次动作的预测位置与执行结果逐项一致，最大 yaw 残差约 8.28e-7 rad。目标可见帧由 35/124 增至 84/124，最终距离由 7.193 m 降至 1.726 m；保存的距离碰撞代理及目标—机器人接触指标均为零。该项是教师侧验证，不代表部署运动许可或主动搜索已完成。

教师的整体成功指标仍为 0，动作后跟随计数仅 12/123。原因已经逐帧复算：基准中名为 `facing` 的条件实际是 `10000 < 目标语义像素数 < 44236.8`，再结合距离不大于 3 m。84 个可见帧中只有 12 个达到面积门槛。不能直接将该指标解释为朝向，也不能修改评分门槛来宣布成功。面积反馈 V4 已通过 44 项远端 CPU 测试，并完成同例 123 动作、124 帧的真实试跑，工作进程正常回收。其效果正在独立核验，尚不能宣布成功或新的性能提升。

48 个实际运行缓存均存在并成功加载；它们全部不同于场景旁的网格，41 个场景的连通区域数量也不同。新采集计划已固定真实缓存，运行时保存实际加载的网格并核对。该证据不能追认此前 V1/V2 未冻结的缓存。

**首轮感知训练的结果继续保留。** 此前从官方 DA3-SMALL 通用骨干开始、重新初始化任务层，在单个干净来源场景完成三组各 256 步的对照。原四个永久开发验证场景的相同原始 RGB 上，三组各推理 225 次，其中 48 帧有可见框：

| 方案 | 框平均 IoU | IoU≥0.5 | 置信度≥0.9 且 IoU≥0.5 |
|---|---:|---:|---:|
| pooled + SmoothL1 | 0.143 | 2/48 | 2/48 |
| pooled + L1/GIoU | 0.145 | 1/48 | 1/48 |
| 空间读出 + L1/GIoU/空间分类 | 0.341 | 15/48 | 6/48 |

空间读出在这次小规模诊断中改善定位，但可见性仍漏检 25/48，未接入新的完整导航闭环。旧 Phase2 权重仅作开发回归：祖先清单与官方验证场景有 9 个标识重合，历史逐样本记录不完整，不能认证其干净来源。

下一轮多场景训练程序已编写并进入独立审查，尚未启动该轮优化器。计划使用刚准入的 34 场景，三组各 2,048 步、batch 8，先均匀选场景再选帧，共用完整采样顺序。官方 DA3 完全冻结，任务层重新初始化，唯一模型输入仍是首次 RGB/框与四帧因果 RGB。先测真实 8 步前向/反向耗时而不更新参数，再执行固定预算；最终检查点进入原四场景验证。训练集合约 90.5% 为可见帧，将同时检查消失时误报和可见时漏检，不能仅看框 IoU。

**LightNav 依赖传输仍在原后台任务中继续。** 固定源码、独立 Python 环境和 181 个 wheel 的离线安装链保持独立；主权重约 9.70 GB 尚未传输，尚无 LightNav GPU 推理或共同基线成绩。已准备小缓存分块权重中转方案，等依赖阶段结束后执行。

完整 EVT 清单仍为三任务各 1,405 集、共 4,215 集，尚未正式评测。公开参考长度的 52 个缺项及回退规则已记录。后续还须统一目标指定、相机、动作、逐集角色和评分条件；新感知拟合和教师修复均不能代替同条件完整 SOTA 结果。

主要证据：`simulation_sota_review/clean_teacher_pilot_v3/independent_review_v1/`、`runtime_cache_inventory_v1/`、`clean48_collection_v1/`；`perception_validation_review/clean48_perception_data_v1/`、`clean48_perception_fit_v1/`。本轮训练、仿真及数据产物均独立保存，未覆盖工作回归检查点。

## 仿真阶段进展（2026-09-14 17:14）

34 场景的三组感知训练和固定开发验证已经完成。B1（pooled + L1/GIoU）在这轮开发集上表现最好，框平均 IoU 从上一轮的 0.1454 提高到 0.4132。当前尚无新训练模型的导航闭环或完整同条件 SOTA 成绩。原项目的任意目标、纯视觉/纯坐标/混合输入、丢失主动搜索和同目标重获范围保留；暂无硬件不影响当前仿真工作。

三组都从官方完全冻结的 DA3-SMALL 和新初始化任务层出发，每组严格完成 2,048 次更新、batch 8，共 6,144 次更新。34 个场景提供 2,921 个可训练时刻；三组共用同一个 16,384 次抽样台账。没有加载旧任务 checkpoint，没有训练骨干或使用 DA3 特征缓存。完整运行约 26.4 分钟，真实退出码为 0，训练后重新确认源码和骨干不变；GPU0 FLUX 在运行前后均存在。三份最终任务权重已分段下载并逐份验证 SHA。

固定开发验证是 **4 个案例、2 个不同场景**，分别为 `ayhkzj2fehp` 和 `gjhyih4upq9`，与本轮 34 个训练场景没有重合。旧报告把 case 数写成场景数，旧证据保留，此处更正。它们仍属于官方 train 内部保留的开发集，不是正式官方 val。

三组使用相同的 225 个非终止 RGB 观测，其中含 4 个初始化观测、48 个有效可见目标框和 177 个目标不可见观测。全部 675 次预测完成，无推理异常；另一份不导入训练器或评分器的 CPU 程序独立重算了逐例 IoU、两档可见性混淆计数和 Brier 分数，全部一致。

| 方案 | 上轮 IoU（1 场景、256 步） | 本轮 IoU（34 场景、2048 步） | 本轮 IoU≥0.5 /48 | 本轮漏检 /48，阈值0.5 | 本轮误报 /177，阈值0.5 |
|---|---:|---:|---:|---:|---:|
| B0 pooled + SmoothL1 | 0.1428 | 0.1050 | 1 | 4 | 103 |
| B1 pooled + L1/GIoU | 0.1454 | 0.4132 | 19 | 4 | 9 |
| D1 空间读出 | 0.3406 | 0.3813 | 18 | 7 | 34 |

阈值 0.9 时，B1 为漏检 4/48、误报 8/177，且 19/48 同时满足置信度≥0.9和 IoU≥0.5。去掉四个初始化观测后，B1 在 221 个观测、44 个可见框上的平均 IoU 为 0.4100，IoU≥0.5 为 18/44。本轮同时增加训练场景和优化器步数，不能把变化单独归因于数据量；B0 的定位和误报明显退步，D1 误报也增加，全部结果均保留。训练集合上 B0/B1/D1 的框 IoU 分别为 0.5127/0.6887/0.7217，不能用这些拟合值替代开发验证结果。

结果图和逐项表：`docs/simulation_sota/CLEAN48_PERCEPTION_COMPARISON.md`、`CLEAN48_PERCEPTION_COMPARISON.png`。完整固定指标另存 `CLEAN48_PERCEPTION_METRICS.json`。

教师 V5 的同例真实仿真和独立审计也已完成。修复内容是：普通远距离低面积目标继续接近，只有边缘截断或已到距离下限时才累计横移重取景资格，面积反馈的目标距离下限设为 1.30 m。原场景、目标、种子、评分器及 V3 动作 guard 均保留。

| 同一固定试例 | V4 | V5 |
|---|---:|---:|
| 最终成功 | 0 | 1 |
| 动作后跟随 | 26/123 | 43/123 |
| 可见观测 | 99/124 | 93/124 |
| 最长连续跟随 | 20 帧 | 17 帧 |
| 最终距离 | 1.795 m | 1.30004 m |

V5 的 262 个封存文件、3,379 个 guard 候选、123 次请求和全部原指标通过独立核验。V4/V5 的 124 个目标位置和世界时刻完全一致；实际世界窗口 5.984 秒，按后帧标记前区间计算的跟随覆盖为 2.104 秒（35.16%）。保存的距离碰撞代理和目标—机器人接触标量均为零，但没有完整接触几何的独立证据。最终成功改善与可见性、最长连续段的退步同时存在，不能据此宣布教师整体或主动搜索合格。

**新的 V5 全 48 固定候选评估正在 GPU3 串行运行。** STT、DT、AT 各 16 个，成员、顺序、种子、目标轨迹配置与实际运行网格和原 V3 批次逐一对照。保留初始化失败和动作检查失败，不替换案例。全部冻结计划、336 个案例源码和 48 个网格均通过独立 CPU 检查；未确认上一进程回收、GPU3 被占用或 FLUX 消失时停止。当前不填尚未完成的批次成功率。

导航训练标签的时间和坐标适配已在 V5 真实轨迹上验证：按实际时间插值为未来 0、0.1、…、0.7 秒的局部米制路径，使用真实 base position 和姿态方向，不能把帧数或归一化动作当速度。123 个非终止 anchor 中，109 个具备完整 0.7 秒轨迹；806 个未来点有效，尾部 55 个点标为无效且填有限零。独立插值/坐标复算最大差约 1.33e-15 m。该文件目前是未准入的几何候选；动作质量、序列 loader、导航模型适配、原地搜索的转向表达仍需补齐，未据此启动导航优化器。

LightNav 原依赖传输任务仍在继续，09:07 UTC 已确认约 3.85/5.18 GB（74.34%）。一个短读 wheel 已补尾并核对原 SHA；主模型权重约 9.70 GB 尚未传输。完整 EVT 三任务各 1,405 集、共 4,215 集清单已固定，正式全量基线和同条件排名尚未运行。输入目标指定、相机和动作条件的差异必须在比较中处理。

新证据包 `takeover_clean48_training_and_v5_evidence_v1.tar.gz` 包含三份最终任务权重、训练/验证/独立复算与 V5 单例证据，165 个成员逐一重新打开验证，SHA256 为 `c0dbe6ce801fae71e10d73822c1072976aef4f6706344da36fe47208b4acdfd2`。它不含完整原始传感器语料、官方骨干大权重或正在运行的新 48 批次结果。

## 仿真阶段进展（2026-09-14 17:46）

发现并定位了训练标签生成链中的实际代码错误：在两例真实 Habitat 实验（DT、AT）中，全部 16 个人物对象的 `visual_scene_nodes` 长度都是 0。旧 `_assign_unique_humanoid_semantic_ids` 只遍历这一列表，因此没有执行任何编号写入。记录中声明的 `2002..2008` 不能当作已生效的渲染身份。

原 34 条训练轨迹中，声明的干扰人物编号在全部图像中都没有像素。全部 11 个 AT 的额外人物与主人物使用同一 avatar。资产原始编号是否被多个物理人物共用、主目标框实际受污染的范围，仍在逐人物隔离渲染中核验；不能仅凭元数据把每一条主标签都判错或判对。

因此，已完成的三组感知训练（每组 2,048 次更新）和 675 次开发预测保留，但所有基于旧声明语义 mask 的数字均标为待实例复核结果。例如 B1 的开发集 IoU 0.413245 是按旧 mask 算出的数值，不能据此认定真实目标定位提高或晋升权重。当前没有正式 SOTA 或合格的新导航模型闭环。原 grant 字节没有修改；root 操作暂停记录另存，当前不启动新的优化器。

已经准备可复用修复模块：遍历人物根节点、骨骼节点和可视节点，给实际可达节点赋 `semantic_id`，保留原主目标编号并为不同人物分配独立编号；赋值失败时恢复原字段。9 项 CPU 测试通过。修复尚待真实渲染验证，未修改旧冻结执行代码。公开的 object/drawable 字段仅作为另行诊断选项，不凭字段名称认定其效果。

第一轮渲染探针暴露了探针自身的恢复问题：不必要地重写未移动的人物状态，会造成约 1e-7 量级矩阵扰动。两例恢复后的 panoptic 完全一致，但 RGB 分别有 5 个与 34 个像素不同。第二版改为仅恢复实际改动的原生位姿；两轮失败输出均完整保留，正在依据原始图像和状态诊断。真实候选渲染未通过前，不声称修复已经有效。

相机核查没有支持旧的“RGB86/panoptic90”猜测。本次 34 条训练和 4 条开发 case 的实际冻结配置都是 384×384、HFOV90、同一传感器挂载。两例真实 probe 的 RGB/panoptic 投影和世界外参矩阵一致。不会据历史描述对现有图像盲目裁切；配置/矩阵一致也不能替代目标实例归属检查。

V5 原 48 例采集已于 17:27 完成，全部进程回收，保存 2,847 帧。与 V3 相同，两版均有 34 例自然结束、13 例零动作 guard 失败和 1 例初始化缺失；全部失败保留在 48 例分母内。96 份案例原文件和原方法数值复算完成，约 5.75 GB、13,042 个已封存文件重验通过。按旧编号链计算的 V3/V5 最终成功分别是 13/48 与 26/48，动作后跟随分别 1026/2955 与 1327/2799。这些仍是旧编号链的数值，不能视为已证明真实主目标跟随改善。agent0/1 的几何距离则直接来自物理位置，可独立验证。

正在准备修正语义后的独立采集版本，保持原 48 个成员、种子、场景和 V5 控制器，保留所有失败。新增实际相机矩阵和节点赋值证据，并处理 reset 后 sensor/metric 缓存刷新。旧数据、checkpoint、原结果和旧包均不覆盖。由于 V5 控制器本身使用 mask，修正标签可能改变动作，优先重新采集；若另行重标旧 RGB，必须证明重放时的原图、时刻与位姿匹配，不能将新轨迹的 mask 粘贴到旧图上。

LightNav 依赖原传输进程和旧 watcher 已退出，失败记录保留。针对 Windows 状态文件替换偶发 WinError5 的有界重试已通过 21 项 CPU 测试，包含真实 Windows 文件占用测试；独立 v4 恢复器已启动（本机 PID17148），继续校验已有 wheel 并补齐传输，安装阶段隐藏 GPU。主权重约 9.70 GB 尚未传输，正式 EVT 基线尚未运行。

项目仍按任意目标、视觉/坐标/混合三模式、丢失主动搜索和同目标重获推进；当前先做仿真，不需要硬件。下一步先证明实例标注正确，完成修正采集与独立检查，再重新训练和运行闭环；最终 SOTA 需要固定数据拆分、同条件强基线和完整评测。

证据入口：`PRIMARY_INSTANCE_BINDING_HOLD_v1.json`、`V5_UNCORRECTED_INSTANCE_BINDING_REVIEW.md`，以及本机审计目录 `simulation_sota_review/semantic_renderer_probe_v1`、`semantic_renderer_probe_v2` 与 `camera_label_alignment_review_v1`。旧 17:14 快照及 `takeover_clean48_training_and_v5_evidence_v1.tar.gz` 不包含本次后发现的实例问题，应与本记录一起阅读。

## 仿真阶段进展（2026-09-14 18:46）

当前没有可信的 SOTA 成绩。旧实例标注链的实际错误已经查明，新增训练、数据准入和权重晋升仍暂停；正在修复并做真实渲染验证。项目范围仍是任意目标、视觉/坐标/混合三模式、丢失主动搜索和同目标重获，当前只关注仿真，无需硬件。

在真实 Habitat-Sim 0.3.1 中，人物的 `visual_scene_nodes` 为空，旧编号分配没有生效。V4 隔离实验进一步确认：AT 场景的 8 个物理人物分别单独放到同一可见位置时，都产生 5,948 个编号 1075 的像素。它们实际共享资产原始编号；这个实验不代表原 reset 帧已经被多个演员共同污染。V4 的根/骨骼/公开可视节点候选也没有改变真实人物像素，不能用于采集。

已对照安装版本的准确 C++ commit 找到原因：skin 绘制节点由独立子树生成，未登记进公开 `visual_scene_nodes`。URDF 模型缓存又以原 filepath 为键，缓存命中不刷新 semantic ID。因此修复改为加载阶段创建每个实例的私有 AO 模板，使用同一份不可变 URDF/GLB/动作资产，并强制重新读取 URDF 模型。该方案不写 SceneNode 的语义字段。原拟议的双字段探针 V5 没有执行；安装版本的语义字段容器初始化还存在潜在边界问题，没有用这条路径继续试验。

V6 在两例真实环境中因 Python 绑定错误而提前失败：读取 `attrs.render_mode` 时，其 C++ enum 未注册。两例都尚未生成 raw frame，也未创建候选环境，不能据此判断加载阶段修复是否有效。两个 worker 均已回收，14 个封存文件、12 个执行源码及 533 个独立引用的复核通过。下一版保留相同物理/相机/隔离协议，先对真实原生 AO 属性做 CPU 预检，再修正这个兼容问题。旧 V1–V6 记录均保留。

真实资格要求包括：8 个初始人物（含未被 episode 替换的 placeholder）和原任务替换路径完整覆盖；逐人隔离渲染的像素身份独立；相同种子/场景的原版与修正版位姿、动作源、相机和人物轮廓可比；记录实际传感器 `semantic_target`。相机核查已证明原配置是 384×384、HFOV90/90；没有依据做 86/90 裁切。

原 V3/V5 的 48 例及三组感知训练、开发集输出均完整保留。旧 mask 口径的 V3/V5 最终成功 13/48 与 26/48，B1 开发 IoU 0.413245，都属于待真实实例复核的历史数值，不能用于权重晋升或 SOTA 声明。旧训练 grant 字节没有修改；当前暂停由 root 操作记录执行，不能声称 loader 已机器撤销旧 grant。

修正后的 48 例采集已经有独立 CPU 草稿，保持原成员、顺序、场景、种子、V5 控制器和失败分母。草稿复用同一加载修复与 factory scope，记录实际 AO 属性/私有模板/源资产、相机和 reset metric 刷新证据；真实渲染尚未合格，因此未冻结或启动。因为 teacher 本身使用 mask，修复可能改变动作，优先重新采集；不能把新轨迹的 mask 粘贴到旧 RGB。开发集与后续感知训练也需要同步修正并 fresh 训练。

LightNav 阶段 B 已完成：181 个锁定依赖全部离线安装，版本一致，pip check 和独立核验通过，原环境元数据及 2,459 个源码未变。新环境尚未完成框架 import、GPU 或模型推理验证。固定版本约 9.70 GB 主权重传输于 18:22 启动，小文件 smoke 通过；随后一个 HTTP 响应提前结束，控制器停止并保留断点。已有 3 个 16 MiB 分块在远端通过 SHA 检查，剩余部分块保留 15,182,861 bytes。正在补充针对短 HTTP EOF 的有界恢复，协议、长度和 SHA 不匹配仍拒绝。旧源码和失败记录保留。

修正协议下，OmTrackVLA 与 LightNav 等基线必须在相同环境和划分重新评测。不能将修正实例后的成绩与他人在原生旧语义链下的公开数值直接排名。后续顺序是：实例修复真实验证 → 修正数据采集及独立复算 → fresh 感知/导航训练 → 纯视觉闭环与同条件强基线比较。

证据：本机审计目录 `semantic_instance_fix_v1/controlled_identity_figure_v1`、`semantic_instance_fix_v1/EVALUATION_IDENTITY_PROTOCOL.md`、`simulation_sota_review/semantic_renderer_probe_v4`、`semantic_renderer_probe_v6/independent_review_v1`、`lightnav_stage_b/INSTALLATION_VERIFICATION.json`、`lightnav_weights_delivery_v1`。以前的快照和原证据包均保留，需连同当前更正阅读。

## 仿真阶段进展（2026-09-14 19:18）

人物实例标签修复已通过真实渲染和独立原始数据核验；修正后的原 48 例仿真采集已启动。当前仍没有 SOTA 成绩，也尚未放行新的 optimizer。旧数据和任务 checkpoint 不覆盖、不冒充实例修复后的结果。

此前真实 Habitat 人物的 visual_scene_nodes 为空，旧编号分配没有生效。更改公开根/骨骼节点同样无法触及 skin 绘制树。当前修复使用每个实例的私有 AO 模板、同一份不可变 URDF/GLB/动作资产，并 force_reload=True，避免按原 URDF filepath 缓存导致 semantic ID 沿用。实际 API 为 sim.metadata_mediator.ao_template_manager；两个未注册 enum getter 改为读取其真实 Configuration 字符串。旧 V6/V7 接口失败记录完整保留；没有执行有潜在字段边界问题的双语义字段探针。

V8 在固定 DT、AT 场景中覆盖全部 8 个人物、初始 placeholder 与原任务替换路径。独立复核通过 542 个封存文件（139,979,974 bytes）、144 个原始 capture、72 次恢复检查、14 个执行源文件和 537 个独立引用。AT 中 8 个演员原先各自产生 5,948 个 ID1075 像素；修复后主目标仍1075，干扰演员各自为2002..2008，单独放在主目标位置时不再产生1075。DT 同样逐人通过。

原版/修正版在相同物理状态下，baseline 和全部单人隔离 RGB 逐像素完全相同；人物轮廓、空背景、相机投影/外参完全相同，初始物理状态最大差为0。各轮原生位姿恢复仍有已记录的浮点残差，满足预先固定的1e-6位姿界限和有界RGB残差协议，不能把初始跨环境“零差异”混称为所有恢复状态都零差异。实际传感器 semantic_target 均为 SemanticSensorTarget.SEMANTIC_ID。

root 已采纳精确 helper/factory 的渲染资格，资格 SHA 25c96e6727ad94a5fc547cf2a3be2a9c6a66655f962412d17d0f44afe417eba2。它仅证明实例构造与渲染身份，未授予动态 teacher 质量或 optimizer 准入。对照图见 docs/simulation_sota/VERIFIED_IDENTITY_FIX.png。

修正后的 48 例批次保持原场景、成员顺序、种子、V5 控制器与失败分母；48/48 远端 CPU 合同检查已通过。Prepared SHA 809501641fee9206ecf7d2fc7b4703f5e29c90d937a033be4978a61a0ad83455，bundle SHA 6feb88bd00271b83821c12438e181e9c53197e06d2c9def5070693ee33d89d7d。root 已单次启动 GPU3 串行采集，首批案例已保存真实帧并回收 worker，原有 guard 失败仍保留。每帧保存实际 AO 属性/源资产引用、相机与语义通道，reset 刷新采用原 detector 和相关 metric 链。批次完成后需再做控制文件封存和独立标签/时间/动作复算。

纯视觉序列接口也有独立 CPU 候选：forward 只有初始化 RGB、初始框和 [B,S,4,3,H,W] 历史 RGB 三输入，S 保留真实 policy-call 序列；每次从 reset 重建状态，内部固定禁用 UWB/binding。13+2 项真实 CPU 测试通过，验证逐步输出、burn-in/TBPTT 梯度、episode 隔离和权重更新后的初始 memory 重算。原 UWB encoder 的常量分支仍保留，不能称为删除 UWB 架构。候选未安装到生产，仍缺合格的序列 loader、真实静态校准与导航训练集成。

LightNav 已完成 181 个依赖的离线安装与独立核验，14/14 必需模块真实 CPU import 通过。GPU7 的 FP32/BF16 64×64 运算误差均为0，vLLM 两个扩展导入成功；该检查最后的16MiB Torch allocator假设过低，实测峰值33,644,544bytes，因此原 worker exit1完整保留，不能把整次检查改称成功。没有运行完整模型或 vLLM 自定义 kernel。GPU worker 已回收。

固定版本约9.70GB权重仍在分块传输，已补充针对短 HTTP EOF 和 TLS EOF 的三次同块有界恢复。证书、其他TLS、HTTP协议、长度、磁盘与SHA失败仍拒绝；已验分块重用，旧失败状态保留。截至本快照前检查，远端至少50块（838,860,800bytes）已通过SHA。只有全部578块、整文件官方SHA与safetensors结构复核通过，才能进入完整模型加载。

旧 V3/V5 成功率、旧 B1 开发 IoU 及三组任务 checkpoint 仍是旧实例编号链下的历史结果。修正版与强基线必须在同一环境/拆分重新运行，不能把修正后的成绩和他人旧原生语义链分数直接做SOTA排名。模型名义4fps、仿真实际世界时间、1/40秒动作积分也须明确分开；不能仅凭WebSocket序号声称时间一致。

项目仍按任意目标、视觉/坐标/混合三模式、丢失主动搜索和同目标重获推进，当前只需仿真。接下来完成修正48数据和固定dev4的独立检查，再 fresh 训练感知与导航，最后运行纯视觉闭环及同条件强基线。当前人物实例修复通过，不代表任意目标能力或三模式最终验收已经完成。
