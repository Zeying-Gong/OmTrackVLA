# OmTrackVLA 实验进展（2026-08-10）

## 一、此前进度

- 完成 Oracle 感知 + Oracle 控制、模块化 RGB-D 感知 + Oracle 控制两组全量基线，并支持多 GPU、多 worker、断点续跑、损坏 JSON 重跑和自动汇总。
- 完成 C1--C3 的 Spot 深度建图、持久障碍地图、动态行人层、A* 路径、carrot 控制和行人历史轨迹，视频可显示 RGB、Depth、Obstacle Map 与诊断信息。
- C3 三任务 Val 表明主要失败来自无碰撞但跟随率不足，控制与跨房间恢复是主要瓶颈。

## 二、今日进展

- 冻结 C4 rule-based 控制基线：在 C3 上加入动态行人最近会遇距离预测、二维撤离和迎面让行，解决了 index 1 的撞人问题。
- 修复 AT index 902 多人/镜像场景的目标框选择：真实目标使用红框，其他行人或镜像区域使用蓝框；逐帧检查未再发生身份互换。
- 检查 AT index 134：动态会车、门区遮挡和跨房间后，局部地图 A* 仍可能选择错误拓扑分支并长距离跟丢。多种门点外推和锁定实验没有稳定收益，均未纳入冻结版本。
- 当前结论：C4 作为可解释的 rule-based control baseline 先跑全量；其跨房间、长期遮挡和动态拓扑失败可用于论证后续 learning-based waypoint/trajectory controller 的必要性。

## 三、推荐命令

单 episode 调试和 C4 全量命令统一维护在 `模块化跟踪笔记.md` 第 7 节。全量 AT 使用：

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=oracle CONTROLLER=map-reactive-c4 \
TASKS=at SPLITS=val \
RENDER_BACKEND=egl \
MAP_MEMORY_FRAMES=0 MAP_ROBOT_RADIUS=0.30 MAP_MIN_STATIC_HITS=2 \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/c4_oracle_pointgoal_map_control_full_val" \
bash eval_oracle_modular_8gpu.sh
```

结果出来后优先按 success、tracking、collision、visible rate 和跨房间恢复失败分类，再决定 learning-based 控制器训练目标。
