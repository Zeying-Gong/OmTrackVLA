# OmTrackVLA 阶段进展汇报

## 一、算法设计

整体采用感知与控制解耦的模块化架构。

### 1. 感知模块

感知模块负责处理 RGB、深度、PointGoal 或 RGB-D 混合输入，并输出统一的目标状态：

```text
relative_xy = (target_forward, target_left)
range_m
bearing_rad
visible
bbox_xyxy
confidence
```

目前有两种主要设置：

- Oracle perception：使用目标的精确相对坐标和 panoptic 信息，用于隔离控制误差；
- RGB-person perception：使用 person detector、OSNet ReID、目标外观和时序关联定位目标。

感知模块给出的是目标相对位置。最终考虑障碍物的可导航 follow goal 由控制模块进一步生成，
而不是直接把机器人规划到行人中心。

### 2. 控制模块

控制模块根据目标状态和障碍信息输出 Spot 底盘连续动作：

```text
(V_x, V_y, V_yaw)
```

当前实现两类模块化控制器：

- C0 `reactive`：根据目标距离、横向偏差和朝向误差进行直接比例控制；
- C1 `map-reactive`：使用深度建立 episodic obstacle map，在膨胀地图上用 A* 搜索
  follow goal，并将局部 carrot waypoint 转换为连续速度。

C1 的地图包含静态层、逐帧更新的动态行人层以及机器人半径膨胀后的规划层。行人不会永久
写入静态地图；多人情况下，所有可见行人作为动态障碍，只有目标行人参与跟踪控制。

## 二、已完成工作

1. 建立 Oracle 感知、RGB-person 感知与多种控制器的实验矩阵；
2. 完成 Oracle navmesh、direct reactive 和 persistent obstacle-map controller 接入；
3. 支持单 episode 调试、全量多 GPU、resume、进度/ETA 和自动汇总；
4. 视频支持 RGB 第一视角、深度图和 obstacle map 三联画；
5. 每步 JSON 记录目标检测、动作、地图模式、clearance、路径长度和可见行人 ID；
6. 增加损坏结果 JSON 检测和对应 episode 重跑机制；
7. 增加多人 semantic instance ID 处理，避免相同 avatar 被错误合并。

## 三、当前结果

C0 在本机三个 val0 调试 episode 上均成功且无碰撞：

| 任务 | Success | Following rate | Collision |
|---|---:|---:|---:|
| STT | 1.0 | 1.0000 | 0.0 |
| DT | 1.0 | 0.9032 | 0.0 |
| AT | 1.0 | 0.9836 | 0.0 |

C1 在 STT/AT val0 上已验证 persistent map、A* 避障和三联画视频链路，代表性结果为：

```text
success=1.0
collision=0.0
following_rate约为0.97--1.00
```

地图已能在椅子、桌子等障碍前触发 `map_astar_waypoint`、`map_blocked_evade` 和
`map_live_depth_recovery`。不过长轨迹、狭窄空间和真正多人同时入镜的 episode 仍可能出现
跟丢或碰撞，因此 C1 当前应视为可运行 prototype，而不是已经冻结的最终方法。

## 四、C2 地图改进

针对 C1 中地面被误标为障碍、持久地图噪声累积的问题，C2 做了两项改动：

1. 使用 Spot jaw depth 相机实际安装高度约 `0.24m`，并提高最低障碍高度阈值，重新进行
   ground/ceiling height filtering；
2. 增加静态地图记忆长度：`MAP_MEMORY_FRAMES=N` 只保留最近 N 帧，`MAP_MEMORY_FRAMES=0`
   保留整个 episode，默认 C2 为最近 4 帧。
3. 增加静态命中门槛：C2 默认 `MAP_MIN_STATIC_HITS=2`，单帧深度噪声不会立即成为规划障碍。

此外，C2 对每个深度返回的相机射线增加 free-space clearing：从机器人到深度端点之间的格子
作为可通行证据，只有端点作为障碍证据。静态占据和 free-space 证据按比例竞争，避免家具边缘
在多帧累积后连接成一整面人工墙，给 A* 留出实际可通行通道。

C2 调试控制器为 `map-reactive-c2`，每步额外记录地面过滤点数、天花板过滤点数和 free-space
清理统计，视频地图
标题会显示当前 memory 设置。对 `dataset-index=49` 的 AT episode，C2 的 4 帧记忆版本
相比 12 帧版本减少了地图占据数量并保持 `collision=0`，但该困难 episode 仍未达到最终
following success，说明地图误检不是唯一瓶颈，还需要继续处理 A* 路径滞回和目标丢失恢复。

## 五、下一步计划

1. 建立按场景、轨迹长度、障碍密度和人数分层的 train/dev 回归集；
2. 补充真正多人同时入镜的视频，统计 ID switch、目标 precision/recall 和恢复延迟；
3. 改进 rolling map、free-space ray clearing、静态证据衰减、动态 TTL 和速度预测；
4. 将 C1 接入 RGB-person 感知，得到完整模块化闭环 D；
5. 根据 A/B/C/D 的差距决定后续感知训练、行为克隆、DAgger 或控制残差学习方案；
6. 最终冻结方法后，用独立输出目录完成三任务全量 Val 和置信区间统计。
