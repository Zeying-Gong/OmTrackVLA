# OmTrackVLA 实验说明（2026-08-04）

所有命令均在仓库根目录执行，并确保当前 Conda 环境可以直接运行 `python`。

## 1. 运行约定

### 1.1 单 episode 调试

调试命令固定使用：

- `--dataset-index 0`：只跑一个 episode；
- `--shard-id 0 --num-shards 1`：不做数据分片；
- `--no-resume`：重复执行时覆盖该 episode 的旧结果；
- `--save-steps`：在 episode JSON 中保存逐步诊断；
- 默认保存视频。

下面示例使用 `CUDA_VISIBLE_DEVICES=0`。如果调度系统分配的卡号不同，请改成实际卡号。

### 1.2 全量评测

全量命令统一使用 `eval_oracle_modular_8gpu.sh`。脚本会优先读取
`CUDA_VISIBLE_DEVICES`；没有设置时，通过 `nvidia-smi` 使用所有可见 GPU。
`NUM_WORKERS` 表示每张 GPU 同时运行的 worker 数，下面的全量命令使用
`NUM_WORKERS=2`。脚本默认令 shard 数等于 GPU 数乘以 `NUM_WORKERS`，无需手动填写
`GPU_LIST` 和 `NUM_SHARDS`。资源紧张或使用较重的感知模型时，可以改为 `NUM_WORKERS=1`。

全量评测默认支持断点续跑。已有完整 JSON 和视频的 episode 会跳过；需要从头重跑时，
请更换 `OUTPUT_ROOT`。

运行期间主终端每 15 秒打印聚合进度，包括已完成/总 episode 数、百分比、本次新增数、
运行速度、已用时间和 ETA。可以通过 `PROGRESS_INTERVAL=30` 调整打印间隔。

Resume 的完整性判断与运行参数一致：`SAVE_VIDEO=1` 时必须同时存在合法 JSON 和非空视频；
`SAVE_VIDEO=0` 时只检查 JSON。默认 first-attempt 评测会保留成功和失败 episode，避免只统计
成功样本；Python/native 错误产生的不完整结果会在下次执行同一命令时继续补跑。

## 2. Oracle 感知 + Oracle 控制

Oracle 感知直接使用仿真真值，不使用深度图做目标定位，因此视频只保存第一视角 RGB。

### 2.1 STT Val

#### 单 episode 调试

```bash
CUDA_VISIBLE_DEVICES=0 python -u oracle_modular_batch.py \
  --task stt --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root "$PWD/outputs/oracle_debug" \
  --perception oracle \
  --save-steps --no-resume
```

#### STT 全量评测（使用所有可见 GPU）

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=oracle \
TASKS=stt SPLITS=val \
RENDER_BACKEND=egl \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/oracle_full_val" \
bash eval_oracle_modular_8gpu.sh
```

### 2.2 DT Val

#### 单 episode 调试

```bash
CUDA_VISIBLE_DEVICES=0 python -u oracle_modular_batch.py \
  --task dt --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root "$PWD/outputs/oracle_debug" \
  --perception oracle \
  --save-steps --no-resume
```

#### DT 全量评测（使用所有可见 GPU）

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=oracle \
TASKS=dt SPLITS=val \
RENDER_BACKEND=egl \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/oracle_full_val" \
bash eval_oracle_modular_8gpu.sh
```

### 2.3 AT Val

#### 单 episode 调试

```bash
CUDA_VISIBLE_DEVICES=0 python -u oracle_modular_batch.py \
  --task at --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root "$PWD/outputs/oracle_debug" \
  --perception oracle \
  --save-steps --no-resume
```

#### AT 全量评测（使用所有可见 GPU）

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=oracle \
TASKS=at SPLITS=val \
RENDER_BACKEND=egl \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/oracle_full_val" \
bash eval_oracle_modular_8gpu.sh
```

## 3. RGB 模型感知 + Oracle 控制 Baseline

RGB baseline 使用第一视角 RGB 和深度传感器。视频为左右双视图：左侧是带目标框与
控制信息的 RGB，右侧是同一时刻的深度图（近处暖色、远处冷色）。

### 3.1 STT Val

#### 单 episode 调试

```bash
CUDA_VISIBLE_DEVICES=0 python -u oracle_modular_batch.py \
  --task stt --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root "$PWD/outputs/rgb_person_debug" \
  --perception rgb-person --person-score-threshold 0.30 \
  --save-steps --no-resume
```

#### STT 全量评测（使用所有可见 GPU）

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=rgb-person \
TASKS=stt SPLITS=val \
RENDER_BACKEND=egl \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/rgb_person_full_val" \
bash eval_oracle_modular_8gpu.sh
```

### 3.2 DT Val

DT 的 `target-initialization=auto` 会自动选择 `goal-crop`，用目标 RGB crop 初始化身份。

#### 单 episode 调试

```bash
CUDA_VISIBLE_DEVICES=0 python -u oracle_modular_batch.py \
  --task dt --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root "$PWD/outputs/rgb_person_debug" \
  --perception rgb-person --person-score-threshold 0.30 \
  --target-initialization auto \
  --save-steps --no-resume
```

#### DT 全量评测（使用所有可见 GPU）

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=rgb-person \
TASKS=dt SPLITS=val \
TARGET_INITIALIZATION=auto \
RENDER_BACKEND=egl \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/rgb_person_full_val" \
bash eval_oracle_modular_8gpu.sh
```

### 3.3 AT Val

#### 单 episode 调试

```bash
CUDA_VISIBLE_DEVICES=0 python -u oracle_modular_batch.py \
  --task at --split val --shard-id 0 --num-shards 1 \
  --dataset-index 0 \
  --output-root "$PWD/outputs/rgb_person_debug" \
  --perception rgb-person --person-score-threshold 0.30 \
  --save-steps --no-resume
```

固定避让方向复现时，可以在命令末尾增加 `--evasion-side 1.0` 或
`--evasion-side -1.0`；正常全量 first-attempt 评测不要固定方向。

#### AT 全量评测（使用所有可见 GPU）

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=rgb-person \
TASKS=at SPLITS=val \
RENDER_BACKEND=egl \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/rgb_person_full_val" \
bash eval_oracle_modular_8gpu.sh
```

## 4. 三任务联合全量评测

### 4.1 Oracle：STT + DT + AT 全量 Val

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=oracle \
TASKS=stt,dt,at SPLITS=val \
RENDER_BACKEND=egl \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/oracle_full_val" \
bash eval_oracle_modular_8gpu.sh
```

### 4.2 RGB baseline：STT + DT + AT 全量 Val

```bash
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=2 \
PERCEPTION=rgb-person \
TASKS=stt,dt,at SPLITS=val \
TARGET_INITIALIZATION=auto \
RENDER_BACKEND=egl \
GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/rgb_person_full_val" \
bash eval_oracle_modular_8gpu.sh
```

需要同时跑 Train 和 Val 时，将任一全量命令中的 `SPLITS=val` 改为：

```bash
SPLITS=train,val
```

## 5. GPU、渲染与输出说明

### 5.1 GPU 自适应示例

- `CUDA_VISIBLE_DEVICES=0 NUM_WORKERS=2`：使用 1 张卡、2 个 worker、2 个 shard；
- `CUDA_VISIBLE_DEVICES=0,1,2,3 NUM_WORKERS=2`：使用 4 张卡、8 个 worker、8 个 shard；
- 未设置 `CUDA_VISIBLE_DEVICES`：自动使用 `nvidia-smi` 检测到的所有 GPU。

每个 worker 同一时刻运行一个 episode；同一张 GPU 上的多个 worker 分别运行不同 shard，
不会重复处理 episode。增加 worker 数不只增加显存占用，也会增加 CPU、主存、视频编码和
磁盘 I/O 压力，建议根据进度输出中的吞吐量比较 `NUM_WORKERS=1/2/4`。

需要手动覆盖时：

```bash
GPU_LIST=0,1 NUM_SHARDS=2 \
PYTHON_BIN="$(command -v python)" \
NUM_WORKERS=1 \
PERCEPTION=rgb-person TASKS=at SPLITS=val \
RENDER_BACKEND=egl GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/rgb_person_full_val" \
bash eval_oracle_modular_8gpu.sh
```

### 5.2 渲染后端

上述命令默认显式使用 `RENDER_BACKEND=egl`。没有 EGL 的机器可以改为
`RENDER_BACKEND=xvfb`，但必须先安装 `Xvfb`，并将
`OMTRACKVLA_HAB_SIM_GLX_ROOT` 设置为该机器真实的 Habitat-Sim GLX build 路径。

### 5.3 输出目录

结果保存在：

```text
OUTPUT_ROOT/<task>/<split>/episodes/*.json
OUTPUT_ROOT/<task>/<split>/videos/*.mp4
OUTPUT_ROOT/<task>/<split>/shard_*_summary.json
OUTPUT_ROOT/<task>/<split>/episodes.csv
OUTPUT_ROOT/<task>/<split>/summary.json
OUTPUT_ROOT/episodes.csv
OUTPUT_ROOT/summary.json
```

其中 `<task>/<split>/episodes.csv` 和 `summary.json` 只包含对应任务与数据划分；根目录的
`episodes.csv` 和 `summary.json` 是扫描当前 `OUTPUT_ROOT` 下所有任务后生成的总表。使用
同一个 `OUTPUT_ROOT` 依次运行 AT 和 DT 不会丢失 AT：AT、DT 的局部表分别保存，根级总表
则在每次汇总时重建并包含两者。

日志保存在 `logs/oracle_modular_dataset_<RUN_ID>/`。全量视频占用空间较大；不需要视频时
设置 `SAVE_VIDEO=0`。
