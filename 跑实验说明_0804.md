# OmTrackVLA 实验说明（2026-08-04）

## 1. 调试 oracle 在本机能否执行

### STT任务-oracle算法-Val split

```
python oracle_modular_batch.py     --task stt     --split val     --shard-id 0     --num-shards 1     --output-root ./output     > /data/nfs/share/OmTrackVLA/temp/log_$(date +%Y%m%d_%H%M%S).txt 2>&1
```

### DT任务-oracle算法-Val split

```
python oracle_modular_batch.py     --task dt     --split val     --shard-id 0     --num-shards 1     --output-root ./output     > /data/nfs/share/OmTrackVLA/temp/log_$(date +%Y%m%d_%H%M%S).txt 2>&1
```

### AT任务-oracle算法-Val split

```
python oracle_modular_batch.py     --task at     --split val     --shard-id 0     --num-shards 1     --output-root ./output     > /data/nfs/share/OmTrackVLA/temp/log_$(date +%Y%m%d_%H%M%S).txt 2>&1
```

## 2. 调试 RGB模型感知+oracle控制 Baseline 在本机能否执行

### STT任务-RGB模型感知+oracle控制算法-Val split

```
# 单episode调试
python oracle_modular_batch.py \
    --task stt --split val --shard-id 0 --num-shards 1 \
    --dataset-index 0 --output-root ./output_rgb \
    --perception rgb-person --person-score-threshold 0.30
```

### DT任务-RGB模型感知+oracle控制算法-Val split

```
# 单episode调试
python oracle_modular_batch.py \
    --task dt --split val --shard-id 0 --num-shards 1 \
    --dataset-index 0 --output-root ./output_rgb \
    --perception rgb-person --person-score-threshold 0.30
```

### AT任务-RGB模型感知+oracle控制算法-Val split

```
# 单episode调试
python oracle_modular_batch.py \
    --task at --split val --shard-id 0 --num-shards 1 \
    --dataset-index 0 --output-root ./output_rgb \
    --perception rgb-person --person-score-threshold 0.30
```

```
# 批量评测
PERCEPTION=rgb-person TASK=stt bash eval_oracle_indices_8gpu.sh
```

## 4. RGB Baseline 全量推理

`eval_oracle_modular_8gpu.sh` 会自动读取 `CUDA_VISIBLE_DEVICES`；如果没有设置，
则使用 `nvidia-smi` 检测当前可见 GPU。默认每张卡启动一个 shard，因此同一条命令
可以用于单卡或多卡，`NUM_SHARDS` 无需手动填写。RGB-person 的视频会同时保存第一视角
RGB（左侧）和对应深度图（右侧）。

### 4.1 全量 Val（STT、DT、AT）

```bash
PYTHON_BIN="$(command -v python)" \
PERCEPTION=rgb-person \
TASKS=stt,dt,at \
SPLITS=val \
RENDER_BACKEND=egl \
GPU_BURN_DUTY=0 \
SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/rgb_person_full_val" \
bash eval_oracle_modular_8gpu.sh
```

### 4.2 全量 Train + Val

```bash
PYTHON_BIN="$(command -v python)" \
PERCEPTION=rgb-person \
TASKS=stt,dt,at \
SPLITS=train,val \
RENDER_BACKEND=egl \
GPU_BURN_DUTY=0 \
SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/rgb_person_full_train_val" \
bash eval_oracle_modular_8gpu.sh
```

例如，调度系统只分配一张卡并设置 `CUDA_VISIBLE_DEVICES=0` 时自动使用 1 个 shard；
设置 `CUDA_VISIBLE_DEVICES=0,1,2,3` 时自动使用 4 个 shard。也可以显式覆盖：

```bash
GPU_LIST=0,1 NUM_SHARDS=2 \
PYTHON_BIN="$(command -v python)" \
PERCEPTION=rgb-person TASKS=stt,dt,at SPLITS=val \
RENDER_BACKEND=egl GPU_BURN_DUTY=0 SAVE_VIDEO=1 \
OUTPUT_ROOT="$PWD/outputs/rgb_person_full_val" \
bash eval_oracle_modular_8gpu.sh
```

结果按 `任务/split/episodes` 和 `任务/split/videos` 保存。launcher 默认断点续跑，
已有完整 JSON 和视频的 episode 会跳过；需要从头重跑时应更换 `OUTPUT_ROOT`。
没有 EGL 的机器可以设置 `RENDER_BACKEND=xvfb`，但必须先安装 `Xvfb` 并配置正确的
`OMTRACKVLA_HAB_SIM_GLX_ROOT`。

## 3. 调试 RGB模型感知+oracle控制 Baseline 在本机能否执行

```
# 单episode调试
python oracle_modular_batch.py \
    --task stt --split val --shard-id 0 --num-shards 1 \
    --dataset-index 0 --output-root ./output_rgb \
    --perception rgb-person --person-score-threshold 0.30

# 批量评测
PERCEPTION=rgb-person TASK=stt bash eval_oracle_indices_8gpu.sh
```
